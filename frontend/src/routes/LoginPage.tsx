/**
 * Placeholder — Plan 14-05 ships the real Login page (react-hook-form + zod).
 */
export function LoginPage() {
  return <div className="p-6">LoginPage placeholder — filled by Plan 14-05</div>;
}
