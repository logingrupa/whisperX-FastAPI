---
phase: 19
plan: 15
slug: frontend-test-and-e2e-pass
type: execute
wave: 10
depends_on: [14]
files_modified:
  - tests/conftest.py
autonomous: false
requirements:
  - REFACTOR-07
  - REFACTOR-06
must_haves:
  truths:
    - "tests/conftest.py defines two autouse fixtures: _clear_dependency_overrides + _clear_lru_caches (belt-and-suspenders for Plan 10's per-fixture cleanup)."
    - "Backend pytest suite GREEN (full suite — verifies the autouse additions are wave-safe)."
    - "frontend `bun run test` GREEN."
    - "frontend `bun run test:e2e` GREEN — Set-Cookie attrs byte-identical to pre-refactor."
  artifacts:
    - path: "tests/conftest.py"
      provides: "_clear_dependency_overrides + _clear_lru_caches autouse fixtures"
  key_links:
    - from: "tests/conftest.py autouse fixtures"
      to: "Plan 10 per-fixture cleanup"
      via: "two-tier cleanup safety: per-fixture + autouse"
      pattern: "@pytest\\.fixture\\(autouse=True\\)"
---

<objective>
T-19-15 — final fixture safety net + frontend regression gate. Plan 10 already migrated the 14 integration test fixtures to `app.dependency_overrides[get_db]` and added per-fixture `app.dependency_overrides.clear()` teardown. This plan adds two autouse fixtures in `tests/conftest.py` as belt-and-suspenders against any future fixture that forgets cleanup, and runs the frontend test suite + Playwright e2e to verify the HTTP contract (Set-Cookie attrs) is byte-identical pre/post refactor.

Purpose: REFACTOR-06 (defence-in-depth against override leakage) + REFACTOR-07 (Set-Cookie wire bytes match → frontend e2e green).

This plan has a HUMAN-VERIFY checkpoint because Playwright auto-starts the dev server and any e2e flake needs a human to interpret. If `bun run test:e2e` red, the human routes back to handle_blocker (likely a backend HTTP-contract regression in Plans 04/05/07/11/12).

Output: 2 autouse fixtures added to tests/conftest.py + human-verified frontend regression gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth-di-refactor/19-CONTEXT.md
@.planning/phases/19-auth-di-refactor/19-PATTERNS.md
@tests/conftest.py
</context>

<interfaces>
From 19-PATTERNS.md §"tests/conftest.py":
- _clear_dependency_overrides — autouse, yields then clears overrides on app.main.app
- _clear_lru_caches — autouse, yields then cache_clear()s every services factory

Plan 10 already added per-fixture `app.dependency_overrides.clear()` in fixture teardown. The autouse fixtures here run AFTER the per-fixture teardown for any fixture that forgets — they are a safety net, not the primary cleanup.

Plan 02 added @lru_cache(maxsize=1) singletons in app/core/services.py. Without cache_clear() between tests, a singleton could carry stale state across tests. Plan 14's no-leak test does not require this, but other future tests might.
</interfaces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test fixture lifecycle → next test | Override or singleton state could leak across tests |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-15-01 | Test isolation | Fixture forgets to clear dependency_overrides | mitigate | Autouse `_clear_dependency_overrides` runs after every test. Belt-and-suspenders with Plan 10's per-fixture teardown. |
| T-19-15-02 | Test isolation | @lru_cache singletons carry stale state across tests | mitigate | Autouse `_clear_lru_caches` runs after every test, cache_clear()s each services factory. |
| T-19-15-03 | Frontend HTTP-contract regression | Backend cookie attrs drift during refactor | mitigate | Playwright e2e suite (Phase 15 lock) compares Set-Cookie wire bytes pre/post. Plan 04 unit-locked the attrs (per INFO 1 below — Plan 04 added a backend Set-Cookie integration test); this plan's Playwright run is the final wire-byte verification. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add autouse cleanup fixtures to tests/conftest.py</name>
  <read_first>
    - tests/conftest.py (full current file — see what fixtures already exist; preserve them)
    - 19-PATTERNS.md §"tests/conftest.py"
    - app/core/services.py (Plan 02 — confirm the @lru_cache factories: get_password_service, get_csrf_service, get_token_service, get_ws_ticket_service, plus any ML-service factories if added)
  </read_first>
  <files>tests/conftest.py</files>
  <action>
    Append two autouse fixtures at the bottom of tests/conftest.py (preserve all existing content):

    ```python
    @pytest.fixture(autouse=True)
    def _clear_dependency_overrides():
        """Test isolation: prevent overrides bleeding across files (Pitfall 6).

        Plan 10 added per-fixture cleanup; this autouse fixture is belt-and-suspenders
        for any fixture that forgets.
        """
        yield
        from app.main import app
        app.dependency_overrides.clear()


    @pytest.fixture(autouse=True)
    def _clear_lru_caches():
        """Test isolation: clear @lru_cache singletons between tests (Pitfall 7).

        Each services factory in app/core/services.py is @lru_cache(maxsize=1).
        Without cache_clear, a stale singleton could leak state across tests.
        """
        yield
        from app.core import services
        services.get_password_service.cache_clear()
        services.get_csrf_service.cache_clear()
        services.get_token_service.cache_clear()
        services.get_ws_ticket_service.cache_clear()
        # Add any other @lru_cache services factories here as Plan 02 evolves.
    ```

    Verify after edit:
    - `grep -c "_clear_dependency_overrides" tests/conftest.py` == 1
    - `grep -c "_clear_lru_caches" tests/conftest.py` == 1
    - `pytest tests/ -x --tb=short` — full suite GREEN (the autouse fixtures should be no-ops in any test that doesn't use overrides or lru-cached services)

    Tiger-style:
    - Two single-purpose autouse fixtures
    - Lazy imports inside fixture body so importing tests/conftest.py doesn't pull all of app.main
    - Each cache_clear() is explicit (no for-loop magic) — easy to grep + audit
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/ -x --tb=short && grep -c "_clear_dependency_overrides" tests/conftest.py | grep -q 1 && grep -c "_clear_lru_caches" tests/conftest.py | grep -q 1</automated>
  </verify>
  <acceptance_criteria>
    - Two autouse fixtures present in tests/conftest.py
    - Full backend pytest suite GREEN
    - Plan 14 no-leak test still passes
    - Plan 10's migrated integration tests still pass (autouse fixtures are no-ops there since per-fixture cleanup already runs)
  </acceptance_criteria>
  <done>
    - Autouse cleanup fixtures in place
    - One commit: `test(19-15): add autouse cleanup fixtures (overrides + lru_cache singletons)`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Frontend tests + Playwright e2e green — REFACTOR-07 gate</name>
  <files>frontend/src/lib/apiClient.ts, frontend/e2e/account-page/*.spec.ts</files>
  <read_first>
    - CLAUDE.md §"Phase 15 e2e suite covers manual UAT items 1-5" — spec table for the 5 Playwright suites
    - frontend/playwright.config.ts — webServer auto-starts `bun run dev`
  </read_first>
  <acceptance_criteria>
    - `cd frontend && bun run test` — ALL GREEN (vitest unit + integration via jsdom + MSW)
    - `cd frontend && bun run test:e2e` — ALL GREEN (Playwright Chromium; 5 specs from CLAUDE.md Phase 15 table)
    - Human reviewer types `approved` after both commands complete green.
    - Any 401/Set-Cookie/CSRF/csrf_token failure: reviewer pastes the failure block; executor routes to backend fix (Plans 04/05/07/11/12).
  </acceptance_criteria>
  <action>
    Pause execution. Present the verification steps below to the user. Wait for the resume-signal text before continuing or marking the plan complete. Do NOT auto-skip — REFACTOR-07 wire-byte equivalence is locked at this gate.
  </action>
  <verify>Human reviewer signals approval (or describes failures) per resume-signal below.</verify>
  <done>Reviewer typed `approved` — frontend vitest + Playwright e2e both green; REFACTOR-07 gate closed.</done>
  <what-built>Backend HTTP contract preserved through entire refactor (Plans 02-14). Plan 10 migrated test fixtures to dependency_overrides; Plan 14 locks no-leak regression. This checkpoint verifies via the frontend test suite + Playwright e2e that Set-Cookie wire bytes are byte-identical pre/post.</what-built>
  <how-to-verify>
    From repo root:
    1. `cd frontend`
    2. `bun install` (if needed)
    3. `bun run test` — vitest unit + integration suite (jsdom + MSW). Expected: ALL GREEN. If RED, the failure points to a backend HTTP-contract regression — route back to handle_blocker.
    4. `bun run test:e2e` — Playwright Chromium e2e suite. Expected: ALL 5 specs GREEN (responsive, upgrade-dialog, delete-account, logout-all-cross-tab, design-parity per CLAUDE.md Phase 15 spec table).
    5. Inspect output: any test failure that mentions "401" / "Set-Cookie" / "CSRF" / "csrf_token" → likely backend regression in Plans 04/05/07/11/12.
    6. If green, type `approved` to continue. If red, paste the failure block and I will route to backend fix (likely Plan 04 cookie attrs OR Plan 05 csrf_protected dep OR Plan 07 router CSRF).
  </how-to-verify>
  <resume-signal>Type `approved` if both `bun run test` and `bun run test:e2e` are green; otherwise paste the first failure block.</resume-signal>
</task>

</tasks>

<verification>
- tests/conftest.py has 2 autouse fixtures
- Full backend suite green
- Frontend vitest + Playwright e2e green
</verification>

<success_criteria>
- Autouse safety net in place; per-fixture cleanup belt-and-suspenders
- Frontend e2e green → REFACTOR-07 closed
- One backend commit + checkpoint signal
- Approved checkpoint signal received from human
</success_criteria>

<output>
Create `.planning/phases/19-auth-di-refactor/19-15-SUMMARY.md` after completion.
</output>
