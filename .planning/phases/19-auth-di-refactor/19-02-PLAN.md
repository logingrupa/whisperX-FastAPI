---
phase: 19
plan: 02
slug: services-module
type: execute
wave: 1
depends_on: [01]
files_modified:
  - app/core/services.py
  - tests/unit/test_services_module.py
autonomous: true
requirements:
  - REFACTOR-05
must_haves:
  truths:
    - "Module-level @lru_cache(maxsize=1) factories exist for password/csrf/token/ws_ticket/file/ML services in NEW app/core/services.py."
    - "Each factory returns the SAME instance across repeated calls (singleton invariant)."
    - "ML factories lazy-import inside the function body so CLI / migration paths do not pay the model load cost."
    - "Existing _container coexists; no other source file is modified by this plan."
  artifacts:
    - path: "app/core/services.py"
      provides: "get_password_service, get_csrf_service, get_token_service, get_ws_ticket_service, get_file_service, get_transcription_service, get_diarization_service, get_alignment_service, get_speaker_assignment_service factories"
      contains: "@lru_cache(maxsize=1)"
      min_lines: 60
    - path: "tests/unit/test_services_module.py"
      provides: "Singleton invariant assertions per factory + cache_clear handle test"
      min_lines: 40
  key_links:
    - from: "app/core/services.py"
      to: "app/services/auth/{password,csrf,token}_service.py + app/services/ws_ticket_service.py + app/services/file_service.py + app/infrastructure/ml/__init__.py"
      via: "import inside @lru_cache factory"
      pattern: "from app\\.services\\.auth"
---

<objective>
T-19-02 — create app/core/services.py with module-level @lru_cache singletons for stateless services. Replaces the dependency_injector Singleton/Factory providers in app/core/container.py for stateless services. Existing container.py keeps working — this plan only adds the new module + a unit test.

Purpose: locks the D1 (drop dependency_injector) replacement pattern in one place. Plans 03+ chain off these factories from the FastAPI Depends graph.

Output: NEW app/core/services.py (≈60-80 LOC) + NEW tests/unit/test_services_module.py (≈40-60 LOC, singleton assertions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth-di-refactor/19-CONTEXT.md
@.planning/phases/19-auth-di-refactor/19-RESEARCH.md
@.planning/phases/19-auth-di-refactor/19-PATTERNS.md
@app/core/container.py
@app/services/ws_ticket_service.py
</context>

<interfaces>
From app/services/auth/password_service.py: `class PasswordService` constructor takes no required args.
From app/services/auth/csrf_service.py: `class CsrfService` constructor takes no required args (or reads settings.auth.CSRF_SECRET internally).
From app/services/auth/token_service.py: `class TokenService(secret: str)` — needs `get_settings().auth.JWT_SECRET.get_secret_value()`.
From app/services/ws_ticket_service.py: `class WsTicketService` — stateful in-memory dict + threading.Lock; MUST be singleton.
From app/services/file_service.py: `class FileService` — stateless.
From app/infrastructure/ml/__init__.py: `WhisperXTranscriptionService`, `WhisperXAlignmentService`, `WhisperXDiarizationService(hf_token=...)`, `WhisperXSpeakerAssignmentService`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit test asserting @lru_cache singleton invariant + cache_clear handle</name>
  <files>tests/unit/test_services_module.py</files>
  <behavior>
    - Test 1: get_password_service() returns the same instance on two consecutive calls (`is` identity).
    - Test 2: get_csrf_service() singleton.
    - Test 3: get_token_service() singleton AND constructed lazily — call get_settings to confirm secret is read at first invocation.
    - Test 4: get_ws_ticket_service() singleton — store a ticket in instance A, retrieve via instance B (same dict).
    - Test 5: get_file_service() singleton.
    - Test 6: cache_clear() handle works — call factory, cache_clear, call again, identity changes.
    - Test 7: ML factories (get_transcription_service / get_alignment_service / get_diarization_service / get_speaker_assignment_service) — verify they are wrapped with @lru_cache (assert `hasattr(get_transcription_service, 'cache_clear')`). Skip-mark or mock the actual ML construction so unit test runs in <100ms (use unittest.mock.patch on the lazy import inside the factory).
  </behavior>
  <action>
    Create tests/unit/test_services_module.py mirroring 19-PATTERNS.md §"Test fixture migration" cleanup pattern. Use plain `pytest` style (no pytest-asyncio); each test is a function returning None. Assertions use `is` for identity, `==` only for value equivalence. Use `unittest.mock.patch("app.infrastructure.ml.WhisperXTranscriptionService")` and similar inside the ML test cases so we never load PyTorch in unit tests.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_services_module.py -x --tb=short</automated>
  </verify>
  <done>
    - 7 tests pass; runtime <2s
    - File written; imports app.core.services (which doesn't exist yet → RED)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement app/core/services.py with @lru_cache(maxsize=1) factories</name>
  <files>app/core/services.py</files>
  <behavior>
    - Module imports stdlib + app.core.config + app.services.auth.{csrf_service,password_service,token_service} + app.services.ws_ticket_service + app.services.file_service at top of file.
    - ML imports happen INSIDE each ML factory body (lazy).
    - Each factory is `@lru_cache(maxsize=1)` decorated.
    - Each factory has a 1-line docstring naming the service it returns.
    - Token service factory reads `get_settings().auth.JWT_SECRET.get_secret_value()`.
    - Diarization factory reads `get_settings().whisper.HF_TOKEN`.
    - File contains exactly 9 factories (password, csrf, token, ws_ticket, file, transcription, diarization, alignment, speaker_assignment).
    - File contains zero `import torch`, zero `import whisperx`, zero `from app.infrastructure.ml import` at module scope.
  </behavior>
  <action>
    Create app/core/services.py. Copy the locked pattern from 19-RESEARCH.md §Q3 lines 156-222 verbatim (then trim/adjust the Diarization signature to match the actual `WhisperXDiarizationService` constructor). Double-check the diarization HF token attr by greping `app/core/config.py` for `HF_TOKEN`. If the attr lives somewhere else, use the actual path; do NOT invent a setting.

    Tiger-style discipline:
    - One factory per service, no nested helpers
    - No `if`/early-return inside factories — they're one-line constructors
    - Module-top imports for non-ML services (lazy only for ML)
    - Type hint return type on every factory
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_services_module.py -x --tb=short && grep -c "@lru_cache(maxsize=1)" app/core/services.py | grep -q 9 && grep -c "^from app.infrastructure.ml" app/core/services.py | grep -q 0</automated>
  </verify>
  <done>
    - All 7 unit tests pass (RED → GREEN)
    - exactly 9 @lru_cache decorators in file
    - zero module-scope ML imports (lazy only)
    - `.venv/Scripts/python.exe -m pytest tests/ --tb=short` still ALL GREEN
  </done>
</task>

</tasks>

<verification>
- `grep -c "@lru_cache(maxsize=1)" app/core/services.py` == 9
- `.venv/Scripts/python.exe -c "from app.core.services import get_password_service; assert get_password_service() is get_password_service()"`
- Full backend pytest suite green
</verification>

<success_criteria>
- app/core/services.py exists with 9 factories, all singleton-tested
- ML services not imported at module load
- Full suite green (existing _container untouched)
- Single atomic commit: `feat(19-02): add app/core/services.py with @lru_cache singletons`
</success_criteria>

<output>
Create `.planning/phases/19-auth-di-refactor/19-02-SUMMARY.md` after completion.
</output>
