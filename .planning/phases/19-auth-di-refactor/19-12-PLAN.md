---
phase: 19
plan: 12
slug: delete-csrf-middleware
type: execute
wave: 7
depends_on: [11]
files_modified:
  - app/main.py
  - app/core/csrf_middleware.py
  - tests/unit/core/test_csrf_middleware.py
autonomous: true
requirements:
  - REFACTOR-03
must_haves:
  truths:
    - "app/core/csrf_middleware.py is DELETED."
    - "app/main.py no longer imports CsrfMiddleware or registers it as middleware."
    - "tests/unit/core/test_csrf_middleware.py is DELETED (tests deleted code)."
    - "Every state-mutating cookie-auth route still 403s when X-CSRF-Token missing/mismatch — verified via Phase 16 CSRF integration test (test_csrf_enforcement.py)."
    - "GET /api/account/me + /health still work without X-CSRF-Token (method gate)."
    - "Bearer-auth POST routes still bypass CSRF (bearer skip)."
  artifacts:
    - path: "app/core/csrf_middleware.py"
      provides: "DELETED"
    - path: "app/main.py"
      provides: "Single middleware (CORSMiddleware) left in stack"
    - path: "tests/unit/core/test_csrf_middleware.py"
      provides: "DELETED"
  key_links:
    - from: "All cookie-auth state-mutating routers"
      to: "Depends(csrf_protected)"
      via: "router-level dependencies=[Depends(csrf_protected)]"
      pattern: "dependencies=\\[Depends\\(csrf_protected\\)\\]"
---

<objective>
T-19-12 — delete CsrfMiddleware after Plan 07 confirmed all cookie-auth state-mutating routers use Depends(csrf_protected). Final middleware stack: CORSMiddleware only. CSRF is now per-router Depends. Atomic with deletion of tests/unit/core/test_csrf_middleware.py (tests deleted code).

Purpose: D4 — eliminate the middleware-direct `_container.csrf_service()` callsite at csrf_middleware.py:67. CSRF surface = Depends graph; greppable.

Output: 1 app/ file DELETED + 1 unit test DELETED + app/main.py imports + middleware-stack edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth-di-refactor/19-CONTEXT.md
@app/core/csrf_middleware.py
@app/main.py
@tests/integration/test_csrf_enforcement.py
@tests/unit/core/test_csrf_middleware.py
</context>

<interfaces>
Plan 07 added `dependencies=[Depends(csrf_protected)]` to:
- account_router (Plan 06)
- key_router
- billing_router (split: webhook router has no CSRF)
- task_router
- ws_ticket_router
And per-route on /auth/logout-all (Phase 15-02 lock).

Routers WITHOUT router-level CSRF (intentional):
- auth_router (anonymous /register /login + per-route CSRF on /logout-all)
- billing_webhook_router (no auth, no CSRF; Stripe-Signature schema check at route level)
- WS upgrade route (ticket-based, not state-mutating in browser model)

Behavioral coverage of csrf_protected dep lives in:
- tests/integration/test_csrf_protected_dep.py (Plan 05)
- tests/integration/test_csrf_enforcement.py (Phase 16-04, MUST stay green)
</interfaces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → API state-mutating cookie-auth | CSRF defence shifts from middleware to per-router Depends |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-12-01 | Bypass | Router accidentally lacks dependencies=[csrf_protected] | mitigate | Plan 07 acceptance_criteria explicitly counted these. This plan's verifier re-greps every cookie-auth state-mutating router for the literal `Depends(csrf_protected)` token. |
| T-19-12-02 | Bypass | New future router added without CSRF dep | accept | Detection is Phase 16 integration test test_csrf_enforcement.py — covers /auth/logout-all + DELETE /api/account/data. Adding new routers without test coverage is a code-review concern, not a Phase 19 structural problem. |
| T-19-12-03 | Order swap | csrf_protected runs before authenticated_user (somehow) | mitigate | csrf_protected has `user: User = Depends(authenticated_user)` as a sub-dependency; FastAPI resolves auth FIRST. Verified by Plan 05 unit test. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Delete CsrfMiddleware module + its unit test + drop registration in main.py</name>
  <read_first>
    - app/core/csrf_middleware.py (full file — 69 lines)
    - app/main.py (focus on lines 198-220 where middleware stack is registered)
    - tests/integration/test_csrf_enforcement.py (Phase 16-04 lock — must still pass after middleware delete)
    - tests/unit/core/test_csrf_middleware.py (verify it tests CsrfMiddleware specifically)
    - Plan 07 commit log to confirm dependencies=[Depends(csrf_protected)] applied to every cookie-auth state-mutating router
  </read_first>
  <files>app/main.py, app/core/csrf_middleware.py, tests/unit/core/test_csrf_middleware.py</files>
  <action>
    Four changes (one atomic commit):

    1. **app/main.py** — drop the import:
       ```python
       from app.core.csrf_middleware import CsrfMiddleware  # DELETE THIS LINE
       ```

    2. **app/main.py** — drop the middleware registration:
       ```python
       app.add_middleware(CsrfMiddleware, container=container)  # DELETE THIS LINE
       ```
       After this delete, the middleware stack contains only CORSMiddleware (and any unrelated infra middleware — verify by listing all `app.add_middleware` calls).

    3. **DELETE file**: `git rm app/core/csrf_middleware.py`

    4. **DELETE unit test**: `git rm tests/unit/core/test_csrf_middleware.py` (tests deleted code; behavioral coverage in tests/integration/test_csrf_protected_dep.py + test_csrf_enforcement.py).

    Tiger-style:
    - One file deleted; two lines removed from main.py; one obsolete unit test removed
    - No new test code — Phase 16-04 + Plan 05 already cover the new dep behaviour
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/integration/test_csrf_enforcement.py tests/integration/test_csrf_protected_dep.py tests/ -x --tb=short && grep -rn "CsrfMiddleware" app/ | wc -l | awk '{exit ($1 != 0)}' && test ! -f app/core/csrf_middleware.py && test ! -f tests/unit/core/test_csrf_middleware.py</automated>
  </verify>
  <acceptance_criteria>
    - `app/core/csrf_middleware.py` does NOT exist (file deleted)
    - `tests/unit/core/test_csrf_middleware.py` does NOT exist
    - `grep -rn "CsrfMiddleware" app/` returns ZERO matches
    - `grep -c "from app.core.csrf_middleware" app/main.py` == 0
    - `grep -c "app.add_middleware(CsrfMiddleware" app/main.py` == 0
    - Phase 16-04 test_csrf_enforcement.py PASSES (confirms the Depends path enforces 403 on missing/mismatch)
    - Phase 19 Plan 05 test_csrf_protected_dep.py PASSES
    - Full backend pytest suite GREEN
  </acceptance_criteria>
  <done>
    - CsrfMiddleware DELETED + obsolete unit test DELETED
    - One commit: `refactor(19-12): delete CsrfMiddleware class — CSRF is now Depends(csrf_protected)`
  </done>
</task>

</tasks>

<verification>
- File deleted; grep ZERO
- CSRF integration tests green
- Obsolete unit test gone
</verification>

<success_criteria>
- D4 invariant enforced structurally
- One atomic commit; suite green per atomic-commit invariant
</success_criteria>

<output>
Create `.planning/phases/19-auth-di-refactor/19-12-SUMMARY.md` after completion.
</output>
