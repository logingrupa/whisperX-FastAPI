---
phase: 19
plan: 06
slug: account-me-route-pilot
type: execute
wave: 3
depends_on: [04, 05]
files_modified:
  - app/api/account_routes.py
autonomous: true
requirements:
  - REFACTOR-03
must_haves:
  truths:
    - "GET /api/account/me uses Depends(authenticated_user) instead of Depends(get_authenticated_user) (legacy)."
    - "GET /api/account/me uses Depends(get_account_service_v2) instead of get_account_service (legacy that calls _container)."
    - "Route-level dependencies=[Depends(csrf_protected)] applied to account_router (DELETE /api/account requires CSRF; GET /me bypasses via method gate)."
    - "All existing account integration tests still pass."
  artifacts:
    - path: "app/api/account_routes.py"
      provides: "Pilot route migrated; pattern reusable by other routers"
  key_links:
    - from: "app/api/account_routes.py"
      to: "app/api/dependencies.py::authenticated_user, csrf_protected, get_account_service_v2"
      via: "Depends() chain"
      pattern: "Depends\\(authenticated_user\\)"
---

<objective>
T-19-06 — pilot the migration on /api/account/me + DELETE /api/account + DELETE /api/account/data. This is the smallest-blast-radius route family (3 endpoints, well-tested in Phase 15). If green here, Plan 07 sweeps the rest.

Purpose: prove the Depends chain works end-to-end against real tests before doing the multi-router sweep. Catch any deviation from the pattern early.

Output: ~30 line delta in app/api/account_routes.py (rename imports + add router-level dependencies=[csrf_protected]).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth-di-refactor/19-CONTEXT.md
@.planning/phases/19-auth-di-refactor/19-PATTERNS.md
@app/api/account_routes.py
@app/api/dependencies.py
@tests/integration/test_account_routes.py
</context>

<interfaces>
From app/api/dependencies.py (Plans 03+04+05):
- authenticated_user (raises 401 if no auth)
- csrf_protected (raises 403 on missing/mismatch X-CSRF-Token for cookie-auth state-mutating)
- get_account_service_v2 (chains off get_db + get_user_repository_v2)

From app/api/account_routes.py current state — 3 routes:
- GET /api/account/me → AccountSummaryResponse
- DELETE /api/account/data → 204
- DELETE /api/account → 204
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate account_routes.py to new Depends chain</name>
  <read_first>
    - app/api/account_routes.py (full file — small, ~120 lines)
    - 19-PATTERNS.md §"app/api/account_routes.py" (lines 345-395) — exact rename pattern
    - tests/integration/test_account_routes.py (note: this fixture currently uses old container.override pattern; do NOT touch the fixture in this plan — Plan 14 owns fixture migration)
  </read_first>
  <files>app/api/account_routes.py</files>
  <action>
    Edit app/api/account_routes.py with three surgical changes:

    1. Update imports — replace:
       ```python
       from app.api.dependencies import get_authenticated_user, get_db_session
       ```
       with:
       ```python
       from app.api.dependencies import authenticated_user, csrf_protected, get_account_service_v2, get_db
       ```
       Drop the local `def get_account_service(...)` inside this file IF it exists (use the new v2 from dependencies.py instead). If the local helper has logic the v2 doesn't replicate, keep it but switch its `Depends(get_db_session)` to `Depends(get_db)`.

    2. Update router declaration to apply CSRF at the router level:
       ```python
       account_router = APIRouter(
           prefix="/api/account",
           tags=["Account"],
           dependencies=[Depends(csrf_protected)],
       )
       ```
       (csrf_protected method-gates GET/HEAD/OPTIONS; only DELETE will trigger the check.)

       Add `from fastapi import Depends` import at top if not already present.

    3. Update each route signature — replace `Depends(get_authenticated_user)` with `Depends(authenticated_user)`. Preserve existing param order. Replace `Depends(get_account_service)` with `Depends(get_account_service_v2)` (or update the local helper as in step 1 — pick one path and document in commit message).

    Tiger-style:
    - No nested-if added
    - No try/except added (route bodies unchanged)
    - Single-purpose edit: imports + router decl + 3 signatures

    DO NOT touch:
    - The route bodies (logic is correct from Phase 13/15)
    - The local AccountService instantiation if it exists in the file
    - Any test file (Plan 14 owns fixture migration)
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/integration/test_account_routes.py -x --tb=short && grep -c "Depends(get_authenticated_user)" app/api/account_routes.py | grep -q 0 && grep -c "Depends(authenticated_user)" app/api/account_routes.py | awk '{exit ($1 < 3)}' </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Depends(authenticated_user)" app/api/account_routes.py` >= 3 (one per route; future-proof if route count grows)
    - `grep -c "Depends(get_authenticated_user)" app/api/account_routes.py` == 0
    - `grep -c "Depends(csrf_protected)" app/api/account_routes.py` == 1 (router-level)
    - `grep -c "_container" app/api/account_routes.py` == 0
    - tests/integration/test_account_routes.py passes
    - Full backend pytest suite GREEN
  </acceptance_criteria>
  <done>
    - 3 routes migrated; CSRF applied at router level
    - account integration tests + full suite green
    - One commit: `refactor(19-06): migrate account_routes to Depends(authenticated_user) + csrf_protected`
  </done>
</task>

</tasks>

<verification>
- All grep counts above match
- Full backend pytest suite green
- DualAuthMiddleware still active (still extracts user.id for routes that haven't migrated yet); double-resolution on /api/account/me is acceptable during migration (the dep ignores middleware state)
</verification>

<success_criteria>
- /api/account/me, DELETE /api/account/data, DELETE /api/account all hit the new Depends chain
- One atomic commit
</success_criteria>

<output>
Create `.planning/phases/19-auth-di-refactor/19-06-SUMMARY.md` after completion.
</output>
