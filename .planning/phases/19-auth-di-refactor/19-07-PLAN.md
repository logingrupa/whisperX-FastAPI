---
phase: 19
plan: 07
slug: route-sweep
type: execute
wave: 4
depends_on: [06]
files_modified:
  - app/api/auth_routes.py
  - app/api/key_routes.py
  - app/api/billing_routes.py
  - app/api/billing_webhook_router.py
  - app/api/task_api.py
  - app/api/ws_ticket_routes.py
  - app/api/__init__.py
  - app/main.py
autonomous: true
requirements:
  - REFACTOR-03
must_haves:
  truths:
    - "Every authenticated HTTP route uses Depends(authenticated_user) — zero `Depends(get_authenticated_user)` (legacy) callsites in app/api/."
    - "Every state-mutating cookie-auth router uses dependencies=[Depends(csrf_protected)] (or per-route equivalent)."
    - "Every service Depends use the _v2 chain (or removed if unused)."
    - "Slowapi @limiter.limit decorators on /auth/register and /auth/login still work — request: Request param preserved as the FIRST positional arg."
    - "ws_ticket_routes.py drops its inline _container.ws_ticket_service() helper and imports get_ws_ticket_service from app.core.services."
  artifacts:
    - path: "app/api/auth_routes.py, app/api/key_routes.py, app/api/billing_routes.py, app/api/task_api.py, app/api/ws_ticket_routes.py"
      provides: "Five routers fully migrated to Depends chain"
  key_links:
    - from: "All migrated routers"
      to: "app/api/dependencies.py::authenticated_user, csrf_protected, get_*_service_v2"
      via: "Depends() at signature"
      pattern: "Depends\\(authenticated_user\\)"
---

<objective>
T-19-07 — sweep all remaining HTTP routers (auth, keys, billing, task_api, ws_ticket) to use the new Depends chain from Plans 03-05. Each file is one logical edit; commit atomically as one commit (or break into two if context budget allows — plan defaults to ONE commit covering all five files since the pattern is identical).

Purpose: collapse the migration window. After this commit, ALL HTTP routes use the new path; only middleware (DualAuth, Csrf) and background/WS still consume `_container.X()`.

Output: ~50 line deltas across 5 files (mostly import + signature renames).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth-di-refactor/19-PATTERNS.md
@app/api/auth_routes.py
@app/api/key_routes.py
@app/api/billing_routes.py
@app/api/task_api.py
@app/api/ws_ticket_routes.py
</context>

<interfaces>
From app/api/dependencies.py (Plans 03-05):
- authenticated_user, authenticated_user_optional, csrf_protected
- get_auth_service_v2, get_key_service_v2, get_account_service_v2 (already used in Plan 06)
- get_scoped_task_repository_v2, get_task_management_service_v2
- get_free_tier_gate_v2, get_usage_event_writer_v2

From app/core/services.py (Plan 02):
- get_ws_ticket_service (singleton)
- get_csrf_service (used inside csrf_protected dep — not directly imported by routes)
</interfaces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → API | Untrusted creds + CSRF tokens cross here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07-01 | DoS | slowapi rate limit lost when refactoring auth routes | mitigate | `request: Request` param remains FIRST positional arg in /auth/register and /auth/login (RESEARCH §Pitfall 5). Verifier grep confirms `request: Request,\s*$` precedes `response: Response` in those signatures. |
| T-19-07-02 | Auth bypass | router-level CSRF accidentally applied to auth routes | mitigate | `auth_router` does NOT add `dependencies=[Depends(csrf_protected)]` at router level — /auth/login + /auth/register must work for ANONYMOUS users (no auth → CSRF check would 401 before CSRF). Per-route dependencies=[csrf_protected] only on /auth/logout-all (was set in Phase 15). |
| T-19-07-03 | Cross-user task access | scoped repo missing on a task route | mitigate | task_api.py uses get_task_management_service_v2 which chains off get_scoped_task_repository_v2 which chains off authenticated_user — single source. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Migrate auth_routes.py + key_routes.py + billing_routes.py</name>
  <read_first>
    - app/api/auth_routes.py (Phase 13-03 + 15-02 lock — request:Request first positional MUST stay)
    - app/api/key_routes.py (Phase 13-04 lock — show-once UX preserved)
    - app/api/billing_routes.py (Phase 13-05 lock — 501 stubs)
    - 19-PATTERNS.md §"app/api/auth_routes.py" (lines 297-341) and §"app/api/key_routes.py" (lines 396-429)
  </read_first>
  <files>app/api/auth_routes.py, app/api/key_routes.py, app/api/billing_routes.py</files>
  <action>
    For EACH of the three files, apply the SAME 3-step delta:

    1. Imports — replace `from app.api.dependencies import get_authenticated_user, get_<service>` with:
       ```python
       from app.api.dependencies import authenticated_user, csrf_protected, get_<service>_v2
       ```
       (Pick the v2 service that matches; e.g. get_auth_service_v2 for auth_routes; get_key_service_v2 for key_routes; no service for billing_routes since stubs.)

    2. Router decl — apply router-level CSRF where appropriate:
       - auth_router: NO router-level CSRF (anonymous routes /register /login). Per-route on /logout-all already from Phase 15-02.
       - key_router: ADD `dependencies=[Depends(csrf_protected)]` (POST/DELETE need CSRF; GET method-gated).
       - billing_router: ADD `dependencies=[Depends(csrf_protected)]` (POST stubs need CSRF; webhook is on PUBLIC_ALLOWLIST today — see step 3).

       NOTE billing webhook: /billing/webhook (Stripe-Signature path) is currently in PUBLIC_ALLOWLIST per 13-05 deviation. With CSRF as a router-level dep, the webhook would 403 on missing X-CSRF-Token. SOLUTION: split billing_router into two — billing_router (with CSRF, /checkout) and billing_webhook_router (no auth, no CSRF, /webhook). OR keep one router and remove the webhook from CSRF via a route-level `dependencies=[]` override (FastAPI does NOT support per-route dep removal — must split).

       Pick the SPLIT approach: billing_router (prefix /billing, deps=[csrf_protected]) → /checkout; billing_webhook_router (prefix /billing, no deps) → /webhook. Update app/api/__init__.py to export both. Plan 10 (middleware delete) will revisit when PUBLIC_ALLOWLIST goes away — webhook will rely on Stripe-Signature schema check at the route level.

    3. Route signatures — replace `Depends(get_authenticated_user)` → `Depends(authenticated_user)`. PRESERVE `request: Request` as FIRST positional arg in /register + /login (slowapi requirement, RESEARCH Pitfall 5). Replace `Depends(get_auth_service)` → `Depends(get_auth_service_v2)`. Same for key_service.

    Tiger-style:
    - One file, one focused edit
    - Zero new try/except blocks
    - Zero changes to route bodies (Phase 13/15 logic preserved)
    - request: Request stays FIRST in slowapi-decorated routes
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/integration/test_auth_routes.py tests/integration/test_key_routes.py tests/integration/test_billing_routes.py -x --tb=short && grep -c "Depends(get_authenticated_user)" app/api/auth_routes.py app/api/key_routes.py app/api/billing_routes.py | awk -F: '{s+=$2} END {exit (s != 0)}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "Depends(get_authenticated_user)" app/api/auth_routes.py app/api/key_routes.py app/api/billing_routes.py` total == 0
    - `grep -rc "Depends(authenticated_user)" app/api/auth_routes.py app/api/key_routes.py app/api/billing_routes.py` total >= 5
    - billing_router split if needed; both exported from app/api/__init__.py
    - request: Request remains first positional in /register + /login signatures (verify with grep `def register\(.*request: Request,` AND `def login\(.*request: Request,`)
    - Phase 13-03 + 13-04 + 13-05 + 15-02 integration tests all pass
  </acceptance_criteria>
  <done>3 router files migrated; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate task_api.py + ws_ticket_routes.py</name>
  <read_first>
    - app/api/task_api.py (Phase 13-07 lock — scoped repo on every task route)
    - app/api/ws_ticket_routes.py (Phase 13-06 lock — 60s single-use ticket)
    - 19-PATTERNS.md §"app/api/ws_ticket_routes.py" (lines 435-468)
  </read_first>
  <files>app/api/task_api.py, app/api/ws_ticket_routes.py</files>
  <action>
    For task_api.py:
    1. Replace `Depends(get_authenticated_user)` → `Depends(authenticated_user)`.
    2. Replace `Depends(get_scoped_task_management_service)` (legacy) → `Depends(get_task_management_service_v2)`.
    3. Apply router-level CSRF: `dependencies=[Depends(csrf_protected)]` on the task_router (DELETE /task/{id}, POST /speech-to-text, etc.).
    4. PRESERVE all existing route bodies + Phase 13-07/13-08 invariants.

    For ws_ticket_routes.py:
    1. DELETE the inline `def get_ws_ticket_service():` factory at lines ~43-57 (the one calling `dependencies._container.ws_ticket_service()`).
    2. Replace it with a top-level import:
       ```python
       from app.core.services import get_ws_ticket_service
       ```
    3. Replace `Depends(get_authenticated_user)` → `Depends(authenticated_user)`.
    4. Apply router-level CSRF: `dependencies=[Depends(csrf_protected)]` (POST /api/ws/ticket).

    Tiger-style:
    - No new logic; just dep rename + inline factory deletion
    - ws_ticket_routes.py loses ~15 LOC (dead inline factory + container null-guard)
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/integration/test_task_api.py tests/integration/test_ws_ticket_routes.py tests/integration/test_per_user_scoping.py -x --tb=short && grep -c "_container" app/api/ws_ticket_routes.py | grep -q 0 && grep -c "Depends(get_authenticated_user)" app/api/task_api.py app/api/ws_ticket_routes.py | awk -F: '{s+=$2} END {exit (s != 0)}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_container" app/api/ws_ticket_routes.py` == 0
    - `grep -c "Depends(authenticated_user)" app/api/task_api.py app/api/ws_ticket_routes.py` total >= 3
    - `grep -c "from app.core.services import get_ws_ticket_service" app/api/ws_ticket_routes.py` == 1
    - Phase 13-06 + 13-07 + 13-08 integration tests pass
    - Full pytest suite GREEN
  </acceptance_criteria>
  <done>5 router files fully migrated; one commit per task or one commit covering both — pick one commit covering all 5 files since the pattern is uniform.</done>
</task>

</tasks>

<verification>
- `grep -rln "Depends(get_authenticated_user)" app/api/` returns NO files (legacy dep gone from app/api/)
- `grep -rln "_container" app/api/` returns ONLY app/api/dependencies.py (until Plan 12)
- Full backend suite green
</verification>

<success_criteria>
- All 5 router files migrated to new Depends chain
- ws_ticket_routes inline `_container` factory deleted
- billing_router split into webhook + non-webhook variants if router-level CSRF requires
- One commit (or two if split for clarity): `refactor(19-07): sweep auth/key/billing/task/ws_ticket routers to Depends chain`
</success_criteria>

<output>
Create `.planning/phases/19-auth-di-refactor/19-07-SUMMARY.md` after completion.
</output>
