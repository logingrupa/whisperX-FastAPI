---
phase: 19
plan: 01
slug: baseline-snapshot
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/baseline_phase19.txt
  - .planning/DEVIATIONS.md
autonomous: true
requirements:
  - REFACTOR-06
must_haves:
  truths:
    - "Frozen pytest collection inventory exists at tests/baseline_phase19.txt for end-of-phase regression diff."
    - "DEVIATIONS.md carries a Phase 13 atomic-cutover lock waiver entry pinned 2026-05-02."
  artifacts:
    - path: "tests/baseline_phase19.txt"
      provides: "pytest --collect-only -q output for full tests/ suite at phase start."
    - path: ".planning/DEVIATIONS.md"
      provides: "Phase 13 lock waiver entry with carry-phase pointer to 19-CONTEXT.md."
  key_links:
    - from: "tests/baseline_phase19.txt"
      to: "T-19-16 final verification gate 5+6"
      via: "diff -u baseline vs end-of-phase pytest --collect-only"
      pattern: "tests/baseline_phase19.txt"
---

<objective>
T-19-01 — pin pytest collection inventory + commit DEVIATIONS.md as the gate-zero artefact for the refactor.

Purpose: end-of-phase regression diff has a fixed reference point so verifier can prove ZERO test was silently dropped during the refactor. DEVIATIONS.md commit is the audit trail for the Phase 13 architectural waiver.

Output: tests/baseline_phase19.txt (pytest collect output, full suite, sorted) + .planning/DEVIATIONS.md (already drafted on disk; this plan commits it as part of the same atomic commit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth-di-refactor/19-CONTEXT.md
@.planning/phases/19-auth-di-refactor/19-VALIDATION.md
@.planning/DEVIATIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate pytest baseline + commit DEVIATIONS waiver as one atomic commit</name>
  <read_first>
    - .planning/phases/19-auth-di-refactor/19-CONTEXT.md (verification gate 5 — baseline snapshot is committed in T-19-01)
    - .planning/DEVIATIONS.md (already exists on disk untracked per `git status`; do NOT rewrite, just `git add`)
    - .planning/phases/19-auth-di-refactor/19-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <files>tests/baseline_phase19.txt, .planning/DEVIATIONS.md</files>
  <action>
    1. Generate baseline: `.venv/Scripts/python.exe -m pytest --collect-only -q > tests/baseline_phase19.txt 2>&1` from repo root. Sort the file in place: read, drop the trailing summary line ("=== N tests collected ==="), keep only `path::Class::method` style lines, sort ascending, write back. Strip any pytest warnings/deprecation lines that vary across runs (lines starting with `<warning>` or matching `^[A-Z][a-zA-Z]+Warning:`). Final file is a deterministic newline-separated list of test node IDs.
    2. Verify file is non-empty and has at least 500 lines: `wc -l tests/baseline_phase19.txt` must be >= 500 (current suite has many integration tests).
    3. Verify .planning/DEVIATIONS.md already exists with a 2026-05-02 entry — if absent, ABORT and surface to user (CONTEXT preparation contract violated).
    4. Stage both files: `git add tests/baseline_phase19.txt .planning/DEVIATIONS.md`.
    5. Commit with message: `chore(19-01): pin pytest baseline + commit DEVIATIONS waiver`.
  </action>
  <verify>
    <automated>test -s tests/baseline_phase19.txt && wc -l tests/baseline_phase19.txt | awk '{exit ($1 < 500)}' && grep -q "Phase 13 atomic-cutover lock waived" .planning/DEVIATIONS.md</automated>
  </verify>
  <done>
    - tests/baseline_phase19.txt exists, >= 500 lines, deterministic sort
    - .planning/DEVIATIONS.md committed with 2026-05-02 entry
    - One commit on branch with both files
  </done>
</task>

</tasks>

<verification>
- `git log -1 --name-only` shows both files in the same commit
- `wc -l tests/baseline_phase19.txt` >= 500
- `grep -c "Phase 13 atomic-cutover lock waived" .planning/DEVIATIONS.md` == 1
</verification>

<success_criteria>
Plan complete when: pytest baseline committed; DEVIATIONS.md committed; suite still green (no source changed).
</success_criteria>

<output>
Create `.planning/phases/19-auth-di-refactor/19-01-SUMMARY.md` after completion.
</output>
