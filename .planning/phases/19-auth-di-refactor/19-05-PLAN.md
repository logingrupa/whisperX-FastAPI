---
phase: 19
plan: 05
slug: csrf-protected-dep
type: execute
wave: 2
depends_on: [04]
files_modified:
  - app/api/dependencies.py
  - tests/integration/test_csrf_protected_dep.py
autonomous: true
requirements:
  - REFACTOR-03
must_haves:
  truths:
    - "csrf_protected Depends raises 403 when state-mutating cookie-auth request lacks X-CSRF-Token header."
    - "csrf_protected raises 403 when X-CSRF-Token header does not match csrf_token cookie."
    - "csrf_protected silently passes (returns None) for GET/HEAD/OPTIONS requests."
    - "csrf_protected silently passes for bearer-authenticated requests (Authorization: Bearer ...)."
    - "csrf_protected silently passes when header matches cookie."
    - "csrf_protected composes WITH authenticated_user — auth runs first, CSRF check runs second."
  artifacts:
    - path: "app/api/dependencies.py"
      provides: "csrf_protected dependency"
    - path: "tests/integration/test_csrf_protected_dep.py"
      provides: "5 cases: missing-header-403, mismatch-header-403, GET-pass, bearer-pass, matching-pass"
      min_lines: 80
  key_links:
    - from: "csrf_protected"
      to: "Depends(authenticated_user) + services.get_csrf_service()"
      via: "auth dep runs first; CSRF service singleton from app.core.services"
      pattern: "user: User = Depends\\(authenticated_user\\)"
---

<objective>
T-19-05 — convert CSRF check from BaseHTTPMiddleware to a per-router Depends. Existing CsrfMiddleware stays registered until Plan 11 (belt+suspenders during migration).

Purpose: D4 — composable with authenticated_user, no inverted PUBLIC_ALLOWLIST, no _container.csrf_service() call.

Output: ~25 LOC added to app/api/dependencies.py + NEW tests/integration/test_csrf_protected_dep.py (~80 LOC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth-di-refactor/19-CONTEXT.md
@.planning/phases/19-auth-di-refactor/19-RESEARCH.md
@.planning/phases/19-auth-di-refactor/19-PATTERNS.md
@app/core/csrf_middleware.py
</context>

<interfaces>
From app/core/csrf_middleware.py:52-69 — current logic to mirror.
From app/services/auth/csrf_service.py: `class CsrfService` with `verify(cookie_token: str, header_token: str) -> bool`.
From app/core/services.py (Plan 02): `get_csrf_service() -> CsrfService` (@lru_cache singleton).
From app/api/dependencies.py (Plan 04): `STATE_MUTATING_METHODS`, `BEARER_PREFIX`, `authenticated_user`.
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Untrusted X-CSRF-Token header + csrf_token cookie cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05-01 | Tampering | csrf_token cookie ↔ header double-submit | mitigate | csrf_service.verify uses constant-time comparison (Phase 11 lock); preserved via singleton import. |
| T-19-05-02 | Spoofing | bearer-skip path | mitigate | csrf_protected returns early when Authorization: Bearer is present — bearer auth has no CSRF surface. Test enforces. |
| T-19-05-03 | Bypass | method gate | mitigate | Only POST/PUT/PATCH/DELETE checked; GET/HEAD/OPTIONS short-circuit. Test enforces. |
| T-19-05-04 | Order swap | csrf-before-auth | mitigate | Depends(authenticated_user) is the first parameter → FastAPI resolves auth FIRST; if auth raises 401, CSRF check never runs. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD test — 5 csrf_protected cases</name>
  <files>tests/integration/test_csrf_protected_dep.py</files>
  <behavior>
    Build slim FastAPI app with one route:
    `POST /test-csrf` with `dependencies=[Depends(csrf_protected)]` returning {"ok": True}.

    Cases:
    1. POST with no X-CSRF-Token header (cookie-auth) → 403 + detail "CSRF token missing"
    2. POST with X-CSRF-Token != csrf_token cookie → 403 + detail "CSRF token mismatch"
    3. GET /test-csrf-get with same dep → 200 (method gate short-circuits)
    4. POST with Authorization: Bearer whsk_... → 200 (bearer skips CSRF)
    5. POST with X-CSRF-Token == csrf_token cookie → 200

    Use register_user helper to seed cookie + csrf token. For case 4, create an API key, then send Authorization header instead of cookie.
  </behavior>
  <action>
    Mirror tests/integration/test_csrf_enforcement.py (Phase 16) structure for the cookie + token issuance helpers. Each case is a separate `def test_*` function. tmp_db_engine + dependency_overrides[get_db] override pattern from 19-PATTERNS.md §"Test fixture migration".
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/integration/test_csrf_protected_dep.py -x --tb=short</automated>
  </verify>
  <done>RED: 5 cases fail — csrf_protected does not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement csrf_protected Depends factory</name>
  <files>app/api/dependencies.py</files>
  <behavior>
    Append to dependencies.py:
    ```python
    def csrf_protected(
        request: Request,
        user: User = Depends(authenticated_user),  # auth runs first (DRT)
    ) -> None:
        if request.method not in STATE_MUTATING_METHODS:
            return
        if request.headers.get("authorization", "").startswith(BEARER_PREFIX):
            return
        cookie_token = request.cookies.get("csrf_token", "")
        header_token = request.headers.get("x-csrf-token", "")
        if not header_token:
            raise HTTPException(status_code=403, detail="CSRF token missing")
        if not services.get_csrf_service().verify(cookie_token, header_token):
            raise HTTPException(status_code=403, detail="CSRF token mismatch")
    ```

    Tiger-style discipline:
    - Four flat early-returns / raises
    - Zero nested-if
    - Self-explanatory names (cookie_token, header_token, not c, h)
    - csrf_service accessed via singleton factory call (NOT _container)
  </behavior>
  <action>
    Code excerpt 19-PATTERNS.md §"CSRF dep" (lines 273-291). Place AFTER authenticated_user_optional in the file. No new imports needed (services already aliased from Plan 03; STATE_MUTATING_METHODS + BEARER_PREFIX already from Plan 04).

    Note on detail strings: KEEP "CSRF token missing" + "CSRF token mismatch" distinct (matches Phase 16 test_csrf_enforcement assertions). Do NOT collapse to single string — Phase 16 tests grep for both.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/integration/test_csrf_protected_dep.py tests/ -x --tb=short && grep -c "def csrf_protected" app/api/dependencies.py | grep -q 1</automated>
  </verify>
  <done>
    - 5 test cases GREEN
    - Full suite GREEN (CsrfMiddleware still registered; new dep not yet applied to any router)
    - csrf_protected defined exactly once
  </done>
</task>

</tasks>

<verification>
- `grep -c "def csrf_protected" app/api/dependencies.py` == 1
- `grep -c "STATE_MUTATING_METHODS" app/api/dependencies.py` >= 2 (declared once + used once)
- `grep -c "_container" app/api/dependencies.py` UNCHANGED from Plan 04 (no new container references introduced)
</verification>

<success_criteria>
- csrf_protected dep ready for router-level application in Plans 06-07
- Full suite GREEN; CsrfMiddleware still active (not deleted yet)
- One commit: `feat(19-05): add csrf_protected Depends factory`
</success_criteria>

<output>
Create `.planning/phases/19-auth-di-refactor/19-05-SUMMARY.md` after completion.
</output>
