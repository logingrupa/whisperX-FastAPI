---
phase: 19
plan: 16
slug: dead-code-sweep
type: execute
wave: 10
depends_on: [14]
files_modified:
  - app/api/dependencies.py
  - app/api/_cookie_helpers.py
  - app/main.py
  - scripts/verify_session_leak_fix.py
  - .planning/phases/19-auth-di-refactor/19-CONTEXT.md
  - .planning/phases/19-auth-di-refactor/19-VALIDATION.md
autonomous: true
requirements:
  - REFACTOR-02
must_haves:
  truths:
    - "PUBLIC_ALLOWLIST + _is_public + _set_state_anonymous helpers DELETED — they were artifacts of the dual-stack era; auth is now per-route Depends."
    - "Inline `try/finally session.close()` blocks in app/api/dependencies.py removed — get_db is now the SOLE close site for HTTP request scope."
    - "Final structural invariant: `grep -rn 'session.close()' app/` returns EXACTLY 1 match: get_db in app/api/dependencies.py."
    - "scripts/verify_session_leak_fix.py preserved per D6 (delete only after 2-week green CI window — NOT this plan)."
    - "CONTEXT.md verification gate 2 wording updated to reflect the actual final state (1 literal session.close() match, since whisperx_wrapper_service uses with-block context manager)."
    - "VALIDATION.md G2 wording updated to match the new gate 2 expectation."
  artifacts:
    - path: "app/api/dependencies.py"
      provides: "Cleaned of legacy try/finally close patterns + dead allowlist helpers"
    - path: "app/api/_cookie_helpers.py"
      provides: "Verified — preserved (Phase 15-01 lock; SESSION_COOKIE/CSRF_COOKIE constants)"
    - path: ".planning/phases/19-auth-di-refactor/19-CONTEXT.md"
      provides: "Verification gate 2 wording reconciled with actual implementation"
    - path: ".planning/phases/19-auth-di-refactor/19-VALIDATION.md"
      provides: "G2 expected-count reconciled with CONTEXT.md gate 2"
  key_links:
    - from: "verifier grep gate G2"
      to: "tests/baseline_phase19.txt"
      via: "structural single-source-close enforcement"
      pattern: "session\\.close\\(\\)"
---

<objective>
T-19-16 — final dead-code sweep + planning-doc gate-wording reconciliation. After Plan 13 deletes container.py, several helpers in app/api/dependencies.py and app/main.py are now unreachable: PUBLIC_ALLOWLIST (was the inverted-allowlist for DualAuthMiddleware), _is_public (helper for that allowlist), _set_state_anonymous (set request.state for public routes). Plus any inline `try: ... finally: session.close()` blocks added in commits 0f7bb09 / 61c9d61 — get_db is the SOLE owner of HTTP request session lifecycle.

In addition: CONTEXT.md gate 2 + VALIDATION.md G2 currently say "exactly TWO matches: one inside get_db, one inside the with SessionLocal() block of whisperx_wrapper_service.py". After Plan 09 rewrote the worker to use `with SessionLocal() as db:` (context-manager), the worker has ZERO literal `session.close()` matches — the manager closes implicitly. Final state: `grep -rn 'session.close()' app/` returns EXACTLY 1 (get_db). Plan 16 owns the doc-gate reconciliation in the same commit so verifier output matches CONTEXT.md verbatim.

Purpose: REFACTOR-02 closure — single source of truth for session close.

Output: dependencies.py + _cookie_helpers.py + main.py cleanup; CONTEXT.md gate 2 + VALIDATION.md G2 wording updated; scripts/verify_session_leak_fix.py PRESERVED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth-di-refactor/19-CONTEXT.md
@.planning/phases/19-auth-di-refactor/19-VALIDATION.md
@app/api/dependencies.py
@app/api/_cookie_helpers.py
@app/main.py
</context>

<interfaces>
Phase 13-02 lock referenced PUBLIC_ALLOWLIST (13 paths) + PUBLIC_PREFIXES (2 prefixes). After Plan 11 deletes DualAuthMiddleware, these constants are dead.

Plan 13 already removed `_container` global, `set_container`, `Container | None` type. This plan finishes the cleanup of helper functions tied to the dual-stack era.

Phase 15-01 deviation lock: `clear_auth_cookies` extracted to `app/api/_cookie_helpers.py` as the public DRY source. PRESERVE this file.

D6 lock: scripts/verify_session_leak_fix.py STAYS for one full release cycle. Plan 16 must NOT delete it.

CONTEXT.md gate 2 current text (line ~230-232):
> 2. `grep -rn 'session\.close()' app/` → exactly TWO matches: one inside `get_db` in `app/api/dependencies.py`, one inside the `with SessionLocal()` block of `whisperx_wrapper_service.py` (background path, no request scope).

After Plan 09's context-manager rewrite, the with-block has NO literal `session.close()` (the manager closes implicitly). Final reconciled wording:
> 2. `grep -rn 'session\.close()' app/` → exactly ONE match: inside `get_db` in `app/api/dependencies.py`. Background task in `whisperx_wrapper_service.py` uses `with SessionLocal() as db:` block (context-manager close, no literal `.close()`).

VALIDATION.md G2 current text (line ~73):
> | G2 | Single session.close site | `grep -rn 'session\.close()' app/` | exactly 2 (get_db + WhisperX background) |

Reconciled:
> | G2 | Single session.close site | `grep -rn 'session\.close()' app/` | exactly 1 (get_db; WhisperX worker uses with-block) |
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Sweep dead allowlist helpers + inline session.close() boilerplate + reconcile CONTEXT/VALIDATION gate 2 wording</name>
  <read_first>
    - app/api/dependencies.py (full file post-Plan-13 state — find any leftover `try: ... finally: ... .close()` outside get_db)
    - app/main.py (full file — verify no PUBLIC_ALLOWLIST imports remain)
    - app/api/_cookie_helpers.py (full file — verify SESSION_COOKIE/CSRF_COOKIE/clear_auth_cookies preserved)
    - .planning/phases/19-auth-di-refactor/19-CONTEXT.md `## Verification gates` section (gates 1-21)
    - .planning/phases/19-auth-di-refactor/19-VALIDATION.md `## Greppable Invariants` section
    - scripts/verify_session_leak_fix.py exists (do NOT delete; D6)
  </read_first>
  <files>app/api/dependencies.py, app/main.py, app/api/_cookie_helpers.py, .planning/phases/19-auth-di-refactor/19-CONTEXT.md, .planning/phases/19-auth-di-refactor/19-VALIDATION.md</files>
  <action>
    Five surgical changes (one atomic commit):

    1. **PUBLIC_ALLOWLIST + helpers**: `grep -rn "PUBLIC_ALLOWLIST\|PUBLIC_PREFIXES\|_is_public\|_set_state_anonymous" app/`. For every match found in app/, delete the symbol + any importer reference. Specifically:
       - dependencies.py / main.py imports → all references removed
       - If a helper function is unused (verified by grep), delete it

    2. **Inline session.close() boilerplate**: `grep -rn "session.close()" app/`. Expected post-Plan-13 + Plan-09 state: only `get_db` in app/api/dependencies.py contains a literal `session.close()` (variable name varies — could be `db.close()` if that's what get_db uses). All other matches are dead.
       For each non-get_db match in app/ (excluding scripts/), inspect:
       - If it's a leftover from commits 0f7bb09 / 61c9d61 inside a now-superseded helper → delete the try/finally
       - If it's inside the WhisperX `with SessionLocal()` block → does NOT exist after Plan 09 (context-manager closes implicitly); if a literal `session.close()` survives, delete it (the with-block already handles cleanup)

    3. **Verify scripts/verify_session_leak_fix.py PRESERVED**: `test -f scripts/verify_session_leak_fix.py` must be true (D6 lock — script kept for one release cycle).

    4. **Update CONTEXT.md gate 2 wording**: edit `.planning/phases/19-auth-di-refactor/19-CONTEXT.md` line ~230-232. Replace:
       ```
       2. `grep -rn 'session\.close()' app/` → exactly TWO matches: one inside
          `get_db` in `app/api/dependencies.py`, one inside the `with
          SessionLocal()` block of `whisperx_wrapper_service.py` (background
          path, no request scope).
       ```
       with:
       ```
       2. `grep -rn 'session\.close()' app/` → exactly ONE match: inside `get_db`
          in `app/api/dependencies.py`. Background task in
          `whisperx_wrapper_service.py` uses `with SessionLocal() as db:` block
          (context-manager close, no literal `.close()`).
       ```

    5. **Update VALIDATION.md G2 row**: edit `.planning/phases/19-auth-di-refactor/19-VALIDATION.md` `## Greppable Invariants` table. Replace G2 row:
       ```
       | G2 | Single session.close site | `grep -rn 'session\.close()' app/` | exactly 2 (get_db + WhisperX background) |
       ```
       with:
       ```
       | G2 | Single session.close site | `grep -rn 'session\.close()' app/` | exactly 1 (get_db; WhisperX worker uses with-block) |
       ```

    Tiger-style:
    - Delete only what verifier-grep proves is dead
    - Don't reformat unrelated regions
    - Doc-gate edits are surgical word-replacements, not rewrites
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/ -x --tb=short && grep -rn "PUBLIC_ALLOWLIST\|_is_public\|_set_state_anonymous" app/ | wc -l | awk '{exit ($1 != 0)}' && grep -rn "session.close()" app/ | wc -l | awk '{exit ($1 != 1)}' && test -f scripts/verify_session_leak_fix.py && grep -c "exactly ONE match" .planning/phases/19-auth-di-refactor/19-CONTEXT.md | awk '{exit ($1 < 1)}' && grep -c "exactly 1 (get_db; WhisperX worker uses with-block)" .planning/phases/19-auth-di-refactor/19-VALIDATION.md | grep -q 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "PUBLIC_ALLOWLIST\|_is_public\|_set_state_anonymous" app/` returns ZERO matches
    - `grep -rn "session.close()" app/` returns EXACTLY 1 match (get_db in app/api/dependencies.py)
    - `test -f scripts/verify_session_leak_fix.py` succeeds (file preserved per D6)
    - app/api/_cookie_helpers.py untouched (Phase 15-01 lock)
    - CONTEXT.md gate 2 says "exactly ONE match" (reconciled with actual state)
    - VALIDATION.md G2 expected column says "exactly 1 (get_db; WhisperX worker uses with-block)"
    - Full backend pytest suite GREEN
    - Plan 14 no-leak regression test still GREEN (no behaviour change)
  </acceptance_criteria>
  <done>
    - Dead allowlist helpers + inline close() boilerplate gone
    - Single session.close() callsite in app/
    - Verify script preserved
    - CONTEXT.md gate 2 + VALIDATION.md G2 wording reconciled with implementation
    - One commit: `chore(19-16): dead code sweep — single session.close() callsite, drop PUBLIC_ALLOWLIST helpers, reconcile gate 2 wording`
  </done>
</task>

</tasks>

<verification>
- All grep counts match acceptance_criteria
- D6 verify script still on disk
- No-leak regression test still green
- Doc-gate wording matches implementation (gate 2 says exactly 1 in both CONTEXT and VALIDATION)
</verification>

<success_criteria>
- REFACTOR-02 invariant tightened: 1 session.close() callsite (vs Phase 13's 11+)
- One atomic commit
</success_criteria>

<output>
Create `.planning/phases/19-auth-di-refactor/19-16-SUMMARY.md` after completion.
</output>
