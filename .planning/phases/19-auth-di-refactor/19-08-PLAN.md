---
phase: 19
plan: 08
slug: websocket-migration
type: execute
wave: 5
depends_on: [02, 03]
files_modified:
  - app/api/websocket_api.py
autonomous: true
requirements:
  - REFACTOR-01
  - REFACTOR-02
must_haves:
  truths:
    - "app/api/websocket_api.py contains zero `_container.X()` references."
    - "WS handler uses `with SessionLocal() as db:` block — context-manager closes session on exit."
    - "ws_ticket_service singleton imported from app.core.services."
    - "Five flat early-return guards preserved verbatim from Phase 13-06."
    - "task.user_id != user.id defence-in-depth check (MID-07) preserved."
  artifacts:
    - path: "app/api/websocket_api.py"
      provides: "WS handler with explicit SessionLocal block + singleton ws_ticket_service"
  key_links:
    - from: "app/api/websocket_api.py"
      to: "SessionLocal + get_ws_ticket_service"
      via: "with SessionLocal() as db: + import from app.core.services"
      pattern: "with SessionLocal\\(\\) as db:"
---

<objective>
T-19-08 — rewrite app/api/websocket_api.py: drop `_container` references, replace with `with SessionLocal() as db:` block (Pattern B from PATTERNS.md). WebSocket scope has no `Depends`; the context-manager owns session cleanup.

Purpose: D1 — eliminate the 2 `_container.X()` callsites at lines 81, 85 of websocket_api.py. Single source for WS session lifecycle.

Output: app/api/websocket_api.py rewritten (~157 LOC → ~150 LOC; the `_container is None` defensive guard goes away with the singleton import).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth-di-refactor/19-CONTEXT.md
@.planning/phases/19-auth-di-refactor/19-RESEARCH.md
@.planning/phases/19-auth-di-refactor/19-PATTERNS.md
@app/api/websocket_api.py
@app/infrastructure/database/connection.py
</context>

<interfaces>
From app/core/services.py (Plan 02): `get_ws_ticket_service() -> WsTicketService`.
From app/infrastructure/database/connection.py: `SessionLocal = sessionmaker(...)` — usable as `with SessionLocal() as db:`.
From app/infrastructure/database/repositories/sqlalchemy_task_repository.py: `SQLAlchemyTaskRepository(session: Session)`.
From app/infrastructure/database/repositories/sqlalchemy_user_repository.py: `SQLAlchemyUserRepository(session)`.
WS_POLICY_VIOLATION constant — verify location via grep.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite websocket_api.py to use SessionLocal context manager + singleton</name>
  <read_first>
    - app/api/websocket_api.py (full file — 157 lines)
    - 19-PATTERNS.md §"app/api/websocket_api.py" (lines 471-523)
    - tests/integration/test_websocket_*.py (Phase 13-06 + Phase 16-05 lock — ticket reuse, expired, cross-user-drift cases)
    - 19-CONTEXT.md "Behaviors to preserve EXACTLY" — WsTicketService Singleton + MID-07 defence
  </read_first>
  <files>app/api/websocket_api.py</files>
  <action>
    Rewrite websocket_api.py applying Pattern B (with SessionLocal() as db:) + drop _container references:

    1. Imports — replace
       ```python
       from app.api import dependencies
       ```
       with
       ```python
       from app.core.services import get_ws_ticket_service
       from app.infrastructure.database.connection import SessionLocal
       from app.infrastructure.database.repositories.sqlalchemy_task_repository import SQLAlchemyTaskRepository
       ```

    2. Replace the `if dependencies._container is None: ... await websocket.close(...)` defensive guard (lines ~71-79) with NOTHING — singleton is always available; no null check needed.

    3. Replace `ticket_service = dependencies._container.ws_ticket_service()` (line ~81) with `ticket_service = get_ws_ticket_service()`.

    4. Replace `task_repo = dependencies._container.task_repository(); try: task = task_repo.get_by_id(task_id); finally: task_repo.session.close()` (lines ~83-89) with:
       ```python
       with SessionLocal() as db:
           task = SQLAlchemyTaskRepository(db).get_by_id(task_id)
       ```
       The session closes automatically on context-manager exit. NO manual close().

    5. Preserve ALL five flat guards (ticket-missing, task-missing, ticket-consume-failed, cross-user-drift MID-07, normal upgrade).

    6. PRESERVE `consumed_user_id == task.user_id` defence-in-depth check (MID-07 — Phase 13-06 lock).

    Tiger-style:
    - Five guards stay flat (no nested-if added)
    - Context-manager owns lifecycle; no try/finally
    - Self-explanatory names — db, task, ticket_service, consumed_user_id
    - Zero `_container` references
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/integration/test_websocket_authentication.py tests/integration/test_ws_ticket_safety.py tests/integration/test_websocket_progress.py tests/ -x --tb=short && grep -c "_container" app/api/websocket_api.py | grep -q 0 && grep -c "with SessionLocal() as db:" app/api/websocket_api.py | grep -q 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_container" app/api/websocket_api.py` == 0
    - `grep -c "with SessionLocal() as db:" app/api/websocket_api.py` == 1
    - `grep -c "session.close()" app/api/websocket_api.py` == 0
    - `grep -c "from app.core.services import get_ws_ticket_service" app/api/websocket_api.py` == 1
    - `grep -c "MID-07\|consumed_user_id == task.user_id" app/api/websocket_api.py` >= 1 (defence-in-depth preserved)
    - Phase 13-06 + Phase 16-05 + Phase 13-08 WS tests all pass
    - Full backend pytest suite GREEN
  </acceptance_criteria>
  <done>
    - websocket_api.py rewritten; _container gone; SessionLocal context-manager block in place
    - One commit: `refactor(19-08): WS handler uses SessionLocal context-manager + ws_ticket_service singleton`
  </done>
</task>

</tasks>

<verification>
- All grep counts match acceptance_criteria
- WS integration tests green
- Manual smoke (optional): connect WS with valid ticket → upgraded; connect with expired → 1008 close
</verification>

<success_criteria>
- 2 `_container.X()` callsites at lines 81, 85 of websocket_api.py eliminated
- One atomic commit
</success_criteria>

<output>
Create `.planning/phases/19-auth-di-refactor/19-08-SUMMARY.md` after completion.
</output>
