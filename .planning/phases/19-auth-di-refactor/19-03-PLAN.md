---
phase: 19
plan: 03
slug: get-db-and-repo-deps
type: execute
wave: 1
depends_on: [01]
files_modified:
  - app/api/dependencies.py
  - tests/unit/test_dependencies_get_db.py
autonomous: true
requirements:
  - REFACTOR-02
  - REFACTOR-01
must_haves:
  truths:
    - "app/api/dependencies.py exposes get_db() generator that yields a SessionLocal() and closes it in finally."
    - "Each repository (User, ApiKey, RateLimit, Task, DeviceFingerprint) is exposed as a get_*_repository function chained off Depends(get_db)."
    - "Each service (auth, key, csrf, rate_limit, free_tier_gate, usage_event_writer, account, task_management) has a new get_*_service function chained off Depends-of-get_db OR Depends-of-singleton from app.core.services."
    - "Existing _container-based functions remain in the file (coexistence) — they will be removed in Plan 12."
    - "No nested-if anywhere in the new helpers (tiger-style)."
  artifacts:
    - path: "app/api/dependencies.py"
      provides: "get_db, get_user_repository, get_api_key_repository, get_rate_limit_repository, get_task_repository_v2, get_device_fingerprint_repository, get_auth_service_v2, get_key_service_v2, get_rate_limit_service_v2, get_account_service_v2, get_free_tier_gate_v2, get_usage_event_writer_v2, get_task_management_service_v2 (suffix `_v2` so old + new coexist)"
      contains: "Depends(get_db)"
    - path: "tests/unit/test_dependencies_get_db.py"
      provides: "Coverage for get_db yield/finally + repo chain shape"
      min_lines: 50
  key_links:
    - from: "app/api/dependencies.py::get_db"
      to: "app/infrastructure/database/connection.py::SessionLocal"
      via: "SessionLocal() construct + close in finally"
      pattern: "SessionLocal\\(\\)"
    - from: "app/api/dependencies.py::get_*_repository"
      to: "Depends(get_db)"
      via: "FastAPI dep cache shares ONE Session per request"
      pattern: "db: Session = Depends\\(get_db\\)"
---

<objective>
T-19-03 — implement get_db generator + chained repository providers + chained service providers in app/api/dependencies.py. Existing _container-based functions are LEFT in the file (coexistence guarantee) so Plans 04-09 can swap routes one wave at a time without breaking anything.

Purpose: locks the D1 + D2 lifecycle — ONE Session per HTTP request, closed in get_db's finally. Repo + service factories chain off Depends(get_db) so FastAPI's per-request dep cache shares the Session across the whole call graph.

Output: append ~80 LOC to app/api/dependencies.py + NEW tests/unit/test_dependencies_get_db.py (~50 LOC). Suffix new functions with `_v2` to disambiguate from existing `get_*` until the route migration sweep deletes the old.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth-di-refactor/19-CONTEXT.md
@.planning/phases/19-auth-di-refactor/19-RESEARCH.md
@.planning/phases/19-auth-di-refactor/19-PATTERNS.md
@app/api/dependencies.py
@app/infrastructure/database/connection.py
</context>

<interfaces>
From app/infrastructure/database/connection.py: `SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)` — call as `SessionLocal()` to get a `Session`.

Repository constructors (verify by `grep -n "def __init__" app/infrastructure/database/repositories/`):
- SQLAlchemyUserRepository(session: Session)
- SQLAlchemyApiKeyRepository(session: Session)
- SQLAlchemyRateLimitRepository(session: Session)
- SQLAlchemyTaskRepository(session: Session)
- SQLAlchemyDeviceFingerprintRepository(session: Session)

Service constructors (already exist; reuse Phase 11/13 signatures):
- AuthService(user_repository, password_service, token_service)
- KeyService(repository, password_service?, ...) — verify in app/services/auth/key_service.py
- RateLimitService(repository)
- AccountService(session, user_repository=None) per 15-03 deviation lock
- FreeTierGate(rate_limit_service)
- UsageEventWriter(session)
- TaskManagementService(repository) per 13-07 deviation lock
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD test — get_db lifecycle + repo chain shape</name>
  <files>tests/unit/test_dependencies_get_db.py</files>
  <behavior>
    - Test 1: get_db() is a generator; first next() returns a Session; calling .close() on generator triggers session.close (mock SessionLocal to count close()).
    - Test 2: Iterating get_db() to exhaustion calls session.close exactly once.
    - Test 3: Exception during iteration still calls session.close (use generator.throw).
    - Test 4: get_user_repository_v2(db=session) returns SQLAlchemyUserRepository bound to that exact session (assert `repo.session is session` if attr exists, else assert via mock).
    - Test 5: get_auth_service_v2 — supply mock user_repo + monkeypatch services.get_password_service / get_token_service; assert returned AuthService has those exact deps.
    - Test 6: assert no get_*_v2 helper invokes `_container` (grep the file from inside the test using AST or string scan).
  </behavior>
  <action>
    Use `unittest.mock` to fake SessionLocal where needed (`monkeypatch.setattr("app.api.dependencies.SessionLocal", fake_sessionlocal)`). Tests must be pure unit — no FastAPI app, no TestClient.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_dependencies_get_db.py -x --tb=short</automated>
  </verify>
  <done>RED: 6 tests fail because new helpers don't exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement get_db + 5 repo providers + 8 service providers (all _v2 suffix)</name>
  <files>app/api/dependencies.py</files>
  <behavior>
    - get_db: 4-line generator (db = SessionLocal(); try: yield db; finally: db.close()).
    - get_user_repository_v2(db=Depends(get_db)) -> IUserRepository: return SQLAlchemyUserRepository(db).
    - Same shape for api_key, rate_limit, task (without scope), device_fingerprint.
    - get_auth_service_v2(user_repo=Depends(get_user_repository_v2)) -> AuthService — wires password_service + token_service from app.core.services.
    - get_key_service_v2 — chain off api_key_repository.
    - get_rate_limit_service_v2 — chain off rate_limit_repository.
    - get_free_tier_gate_v2 — chain off rate_limit_service.
    - get_usage_event_writer_v2(db=Depends(get_db)) — direct session.
    - get_account_service_v2(db=Depends(get_db), user_repo=Depends(get_user_repository_v2)) — passes both per 15-03 deviation.
    - get_task_management_service_v2(scoped_repo=Depends(get_scoped_task_repository_v2)) — see Plan 04 for scoped repo + auth.
    - Add new imports at top: `from sqlalchemy.orm import Session` (already present), `from fastapi import Depends`, `from app.infrastructure.database.connection import SessionLocal`, `from app.core import services` (alias).
    - Module retains `_container` and existing `get_*` functions UNTOUCHED.
    - ZERO nested-if in new functions (each is one return statement OR a yield/finally generator).
  </behavior>
  <action>
    Append the new functions to the END of app/api/dependencies.py (after existing functions). Order: imports section additions first (sorted), then a section header comment `# === Phase 19 v2 providers (Depends chain) ===`, then get_db, then 5 repos, then 8 services. Do NOT touch the scoped task repository in this plan — that depends on authenticated_user (Plan 04). Add a stub `def get_scoped_task_repository_v2(...)` inside this commit ONLY if Plan 04's authenticated_user is defined here — otherwise leave it for Plan 04.

    Decision: put the scoped repo + task management service helpers in Plan 04 alongside authenticated_user. This plan stops at the 5 unscoped repos + 7 stateless services.

    Tiger-style discipline:
    - Each helper is 1-3 lines (factory)
    - get_db is the only function with try/finally
    - No nested-if; verifier grep `grep -cE "    if .*\bif\b" app/api/dependencies.py` must stay at 0 for the new lines
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/unit/test_dependencies_get_db.py tests/ -x --tb=short && grep -c "def get_db()" app/api/dependencies.py | grep -q 1 && grep -c "Depends(get_db)" app/api/dependencies.py | head -1</automated>
  </verify>
  <done>
    - 6 unit tests GREEN
    - Full backend suite still GREEN (no existing call site changed)
    - get_db defined exactly once
    - At least 5 functions chain off Depends(get_db)
  </done>
</task>

</tasks>

<verification>
- `grep -c "def get_db()" app/api/dependencies.py` == 1
- `grep -c "_v2" app/api/dependencies.py` >= 12 (5 repos + 7 services minimum)
- `.venv/Scripts/python.exe -m pytest tests/ --tb=short` ALL GREEN
- `grep -cE "^            if " app/api/dependencies.py` shows no triple-nested-if regression
</verification>

<success_criteria>
- get_db generator owns the only session.close() call site for HTTP request scope
- 5 repo + 7 service _v2 providers exist
- Old _container-based providers untouched (coexistence)
- One commit: `feat(19-03): add get_db + Depends-chain repo/service providers`
</success_criteria>

<output>
Create `.planning/phases/19-auth-di-refactor/19-03-SUMMARY.md` after completion.
</output>
