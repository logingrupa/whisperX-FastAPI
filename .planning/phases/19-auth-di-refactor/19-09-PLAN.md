---
phase: 19
plan: 09
slug: background-task-migration
type: execute
wave: 5
depends_on: [02, 03]
files_modified:
  - app/services/whisperx_wrapper_service.py
autonomous: true
requirements:
  - REFACTOR-01
  - REFACTOR-02
must_haves:
  truths:
    - "app/services/whisperx_wrapper_service.py contains zero `_container.X()` references."
    - "All DB access inside the worker happens within a single `with SessionLocal() as db:` block."
    - "_resolve_user_for_task helper deleted (legacy helper that resolved user via _container)."
    - "NEW _release_slot_if_authed module-scope helper added — flat-guard four early-returns; replaces the previous nested-if in finally (CLAUDE.md tiger-style)."
    - "W1 contract preserved: free_tier_gate.release_concurrency(user) ALWAYS runs in finally on success AND failure paths (Phase 13-08 lock)."
    - "Usage event written on successful transcription (RATE-11 / Phase 13-08 invariant)."
  artifacts:
    - path: "app/services/whisperx_wrapper_service.py"
      provides: "Refactored worker with single SessionLocal block + W1 release"
  key_links:
    - from: "app/services/whisperx_wrapper_service.py::process_audio_common finally"
      to: "free_tier_gate.release_concurrency"
      via: "task.user_id -> user_repo.get_by_id -> release"
      pattern: "release_concurrency"
---

<objective>
T-19-09 — rewrite app/services/whisperx_wrapper_service.py to use a single `with SessionLocal() as db:` block for the worker scope. Eliminates 3 `_container.X()` callsites at lines 310 (user_repository), 376 (free_tier_gate), 377 (usage_event_writer). Background tasks have no Request, no Depends — context-manager is the SQLAlchemy-native lifecycle owner.

Purpose: D1 — drop the last non-API `_container.X()` callsites. The W1 free-tier release contract from Phase 13-08 must survive byte-identical (gate verifier checks both success + failure paths).

Output: app/services/whisperx_wrapper_service.py refactored (~643 LOC; net delta likely -20 LOC after _resolve_user_for_task helper deletion + try/finally consolidation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth-di-refactor/19-CONTEXT.md
@.planning/phases/19-auth-di-refactor/19-RESEARCH.md
@.planning/phases/19-auth-di-refactor/19-PATTERNS.md
@app/services/whisperx_wrapper_service.py
@app/infrastructure/database/connection.py
</context>

<interfaces>
From app/infrastructure/database/connection.py: `SessionLocal`.
From app/infrastructure/database/repositories/sqlalchemy_*: SQLAlchemyTaskRepository, SQLAlchemyUserRepository, SQLAlchemyRateLimitRepository.
From app/services/free_tier_gate.py: `class FreeTierGate(rate_limit_service)` with `.release_concurrency(user)` method.
From app/services/usage_event_writer.py: `class UsageEventWriter(session)`.
From app/services/auth.RateLimitService: `RateLimitService(repository)`.
Phase 13-08 W1 invariant — free_tier_gate.release_concurrency(user) MUST run in `finally`, on success AND failure.
</interfaces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| BackgroundTask scope to DB | Worker session must NOT inherit request session (DetachedInstanceError risk per RESEARCH Pitfall 2) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09-01 | DoS | Concurrency slot leak on failure | mitigate | release_concurrency(user) in finally — runs on RuntimeError/ValueError/KeyError too. Test: tests/integration/test_free_tier_concurrency.py forces a failure and asserts slot freed. |
| T-19-09-02 | DoS | Session-pool exhaustion in worker | mitigate | ONE SessionLocal() per worker invocation; context-manager closes on exit (succeeds, fails, or partial-state). |
| T-19-09-03 | Data integrity | Usage event NOT written on success | mitigate | UsageEventWriter.write_event called inside the `with` block ONLY on the success leg (boolean transcription_succeeded gate). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite process_audio_common to single SessionLocal block + delete _resolve_user_for_task</name>
  <read_first>
    - app/services/whisperx_wrapper_service.py (full file — 643 lines; focus on lines 290-420 where _container references live)
    - 19-PATTERNS.md section "app/services/whisperx_wrapper_service.py" (lines 583-644)
    - 19-RESEARCH.md section "Code Example 3" — exact target shape
    - tests/integration/test_free_tier_concurrency.py (Phase 13-08 W1 contract test)
    - tests/integration/test_usage_event_*.py (RATE-11 invariant)
  </read_first>
  <files>app/services/whisperx_wrapper_service.py</files>
  <action>
    Apply five surgical changes:

    1. DELETE the helper `def _resolve_user_for_task(task_user_id) -> User | None` at lines ~293-319. Its only caller is the worker finally block; inline it inside the with-block.

    2. DELETE the import-inside-function pattern `from app.api.dependencies import _container` (appears at lines ~308 and ~370). Replace with module-top imports of SessionLocal + repositories + services.

    3. Replace `session = SessionLocal(); repository = SQLAlchemyTaskRepository(session)` (line ~363) PLUS the trailing `try: ... free_tier_gate = _container.free_tier_gate(); usage_writer = _container.usage_event_writer(); except Exception: logger.warning(...)` (lines ~370-378) with a single `with SessionLocal() as db:` block. The block:
       - opens db via SessionLocal()
       - constructs repository = SQLAlchemyTaskRepository(db)
       - constructs rate_limit_service = RateLimitService(repository=SQLAlchemyRateLimitRepository(db))
       - constructs free_tier_gate = FreeTierGate(rate_limit_service=rate_limit_service)
       - constructs usage_writer = UsageEventWriter(session=db)
       - constructs user_repo = SQLAlchemyUserRepository(db)
       - sets transcription_succeeded = False
       - try: existing transcription work kept verbatim; sets transcription_succeeded = True at the end
       - except (RuntimeError, ValueError, KeyError) as e: logger.exception("Transcription failed: %s", e); raise
       - finally: call `_release_slot_if_authed(repository, user_repo, params.identifier, free_tier_gate)` (flat-guard helper — see below); if transcription_succeeded and completed_task is not None: usage_writer.write_event(...) with the exact existing call shape (gpu_seconds, file_seconds, model, idempotency_key). Capture completed_task once at the top of the finally via repository.get_by_id(params.identifier) and reuse for both the helper call and the usage_writer guard.

    **Flat-guard helper (added at module scope, NOT a nested function):**

    ```python
    def _release_slot_if_authed(
        repo: SQLAlchemyTaskRepository,
        user_repo: SQLAlchemyUserRepository,
        identifier: str,
        free_tier_gate: FreeTierGate,
    ) -> None:
        """Release the concurrency slot iff the task has an authenticated owner.

        Tiger-style flat-guard: each precondition early-returns; no nested if.
        """
        completed_task = repo.get_by_id(identifier)
        if completed_task is None: return
        if completed_task.user_id is None: return
        user = user_repo.get_by_id(completed_task.user_id)
        if user is None: return
        free_tier_gate.release_concurrency(user)
    ```

    The previous nested-if pattern (`if completed_task: if completed_task.user_id: if user:`) violated CLAUDE.md tiger-style. The helper flattens it to four early-returns at module scope — greppable, testable, no scope drift.
       - context exit closes db automatically; ZERO manual close

    4. PRESERVE every business invariant from the original function: success-path call sequence, failure-path logging, usage-event payload (gpu_seconds, file_seconds, model, idempotency_key), W1 release on both paths.

    5. NO `_container` references anywhere in the file. Module-top imports added:
       - from app.infrastructure.database.connection import SessionLocal
       - from app.infrastructure.database.repositories.sqlalchemy_user_repository import SQLAlchemyUserRepository
       - from app.infrastructure.database.repositories.sqlalchemy_rate_limit_repository import SQLAlchemyRateLimitRepository
       - from app.services.auth.rate_limit_service import RateLimitService
       - from app.services.free_tier_gate import FreeTierGate
       - from app.services.usage_event_writer import UsageEventWriter

    Tiger-style:
    - One context-manager owns lifecycle
    - Module-scope helper `_release_slot_if_authed` (flat-guard, four early-returns)
    - Failure path explicit; W1 release in finally
    - Self-explanatory names (transcription_succeeded, completed_task, user_repo)
    - Zero nested-if in finally — flat-guard helper `_release_slot_if_authed` carries the four early-returns at module scope (greppable, testable, no scope drift)
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/integration/test_free_tier_concurrency.py tests/integration/test_usage_event_writer.py tests/ -x --tb=short && grep -c "_release_slot_if_authed" app/services/whisperx_wrapper_service.py | awk "{exit ($1 < 2)}"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_container" app/services/whisperx_wrapper_service.py` == 0
    - `grep -c "with SessionLocal() as db:" app/services/whisperx_wrapper_service.py` == 1
    - `grep -c "session.close()" app/services/whisperx_wrapper_service.py` == 0
    - `grep -c "_resolve_user_for_task" app/services/whisperx_wrapper_service.py` == 0
    - `grep -c "release_concurrency" app/services/whisperx_wrapper_service.py` >= 1 (W1 release preserved)
    - `grep -c "_release_slot_if_authed" app/services/whisperx_wrapper_service.py` >= 2 (helper definition + finally-block call)
    - Tiger-style: no nested-if in the new region (`grep -cE "^[[:space:]]+if .*:[[:space:]]*$" app/services/whisperx_wrapper_service.py` shows only top-level guards inside the helper, no nested chain)
    - Phase 13-08 W1 contract test (test_free_tier_concurrency.py) PASSES
    - RATE-11 usage_events test PASSES
    - Full backend pytest suite GREEN
  </acceptance_criteria>
  <done>
    - whisperx_wrapper_service.py rewritten; _container gone; SessionLocal context-manager block in place
    - _resolve_user_for_task helper deleted
    - One commit: `refactor(19-09): worker uses with SessionLocal() block; W1 release preserved`
  </done>
</task>

</tasks>

<verification>
- All grep counts match
- Worker tests + suite green
- W1 contract preserved (slot released on success AND failure)
</verification>

<success_criteria>
- 3 `_container.X()` callsites at lines 310, 376, 377 eliminated
- One atomic commit
</success_criteria>

<output>
Create `.planning/phases/19-auth-di-refactor/19-09-SUMMARY.md` after completion.
</output>
