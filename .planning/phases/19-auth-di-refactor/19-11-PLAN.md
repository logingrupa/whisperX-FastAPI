---
phase: 19
plan: 11
slug: delete-dual-auth-and-bearer-middleware
type: execute
wave: 7
depends_on: [10]
files_modified:
  - app/main.py
  - app/core/dual_auth.py
  - app/core/auth.py
  - app/core/feature_flags.py
  - tests/unit/core/test_dual_auth.py
  - tests/unit/core/test_container.py
autonomous: true
requirements:
  - REFACTOR-04
must_haves:
  truths:
    - "app/core/dual_auth.py is DELETED."
    - "app/core/auth.py (BearerAuthMiddleware module) is DELETED."
    - "AUTH_V2_ENABLED feature flag + is_auth_v2_enabled helper are removed from app/core/feature_flags.py."
    - "app/main.py no longer imports DualAuthMiddleware, BearerAuthMiddleware, or is_auth_v2_enabled."
    - "app/main.py production fail-loud guard (lines 257-262) is deleted."
    - "Both AUTH_V2 branches in app/main.py middleware-stack registration are deleted; CORSMiddleware is the only non-Csrf middleware left."
    - "tests/unit/core/test_dual_auth.py is DELETED (tests deleted code — Plan 10 already migrated integration fixtures off DualAuth)."
    - "tests/unit/core/test_container.py is DELETED (tests deleted code — Plan 12 deletes app/core/container.py)."
  artifacts:
    - path: "app/main.py"
      provides: "Single auth wiring; no flag branches"
    - path: "app/core/dual_auth.py"
      provides: "DELETED"
    - path: "app/core/auth.py"
      provides: "DELETED"
    - path: "app/core/feature_flags.py"
      provides: "is_auth_v2_enabled() removed; is_hcaptcha_enabled() preserved"
    - path: "tests/unit/core/test_dual_auth.py"
      provides: "DELETED"
    - path: "tests/unit/core/test_container.py"
      provides: "DELETED"
  key_links:
    - from: "app/main.py middleware stack"
      to: "CORSMiddleware + CsrfMiddleware (still here until Plan 12) + (removed AUTH middleware)"
      via: "single non-flagged registration"
      pattern: "app\\.add_middleware"
---

<objective>
T-19-11 — delete the dead auth middleware files now that all routes use Depends(authenticated_user) AND Plan 10 migrated all integration test fixtures off Container().db_session_factory.override + DualAuthMiddleware. After this commit, the only middleware in the stack is CORSMiddleware + CsrfMiddleware (Plan 12 removes the latter). AUTH_V2_ENABLED flag goes away — V2 is THE auth path.

Atomic-commit invariant: Plan 10's integration-test migration MUST land first (Wave 6); only then can these middleware modules be deleted without breaking pytest collection. Unit tests that test the deleted code (test_dual_auth.py, test_container.py) are deleted in this same commit so the pytest suite stays green.

Purpose: D2 + D3 — collapse the dual-stack to a single stack. Source of truth becomes the FastAPI Depends graph.

Output: 3 app/ files DELETED + 2 unit test files DELETED + app/main.py edited to drop dead imports + flag branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth-di-refactor/19-CONTEXT.md
@.planning/phases/19-auth-di-refactor/19-PATTERNS.md
@app/main.py
@app/core/dual_auth.py
@app/core/auth.py
@app/core/feature_flags.py
@tests/unit/core/test_dual_auth.py
@tests/unit/core/test_container.py
</context>

<interfaces>
From app/main.py (current state, ~336 lines):
- Imports at lines 45-49, 61: BearerAuthMiddleware, Container, CsrfMiddleware, DualAuthMiddleware, is_auth_v2_enabled
- Container instantiation at lines 73-80
- AUTH_V2 middleware-stack branch at lines ~198-208
- Router registration AUTH_V2 gate at lines ~247-253
- Production guard at lines 257-262

From app/core/feature_flags.py: `is_auth_v2_enabled()` + `is_hcaptcha_enabled()` (the latter must survive — Phase 13-01 lock).

Plan 10 prerequisite: every tests/integration/test_*.py that used `Container().db_session_factory.override(...)` or `app.add_middleware(DualAuthMiddleware, ...)` has already been migrated to `app.dependency_overrides[get_db]`. Verify: `grep -rn "DualAuthMiddleware\|BearerAuthMiddleware" tests/integration/` returns ZERO matches.
</interfaces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → API | Dual-stack registration creates ambiguity about which auth path runs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-11-01 | Bypass | Stale BearerAuthMiddleware accidentally re-mounted | mitigate | File DELETE means future imports fail-fast at boot. Verifier grep `grep -rn 'BearerAuthMiddleware' app/` == 0 enforced. |
| T-19-11-02 | Confusion | Both middleware + Depends running auth in parallel | mitigate | DualAuth DELETE — no double-resolution, no contradictory states. Routes already opt-in via Depends(authenticated_user) (Plan 06+07). |
| T-19-11-03 | Bypass | Public route accidentally requires auth | mitigate | PUBLIC_ALLOWLIST goes away — public routes are simply those that don't include the dep. /health, /auth/login, /auth/register, /openapi.json — verify each is reachable without auth via integration test (Phase 13 already covers). |
| T-19-11-04 | Production safety | Old prod fail-loud "refuses to boot when V2 disabled" no longer applies | accept | V2 is THE auth path; flag is gone; the guard is meaningless. Removing it is part of D3. |
| T-19-11-05 | Test inventory regression | Deleting test_dual_auth.py + test_container.py drops test names from pytest --collect-only count | accept | These tests covered code that is itself deleted in this same commit. Plan 17 final verification documents the diff vs baseline_phase19.txt; the deletions are EXPECTED, not a regression. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Delete DualAuth + BearerAuth modules + AUTH_V2 flag + their unit tests; clean app/main.py</name>
  <read_first>
    - app/main.py (full file — focus on lines 45-80, 198-262)
    - app/core/dual_auth.py (verify nothing else imports the module: `grep -rn "from app.core.dual_auth" app/ tests/`. After Plan 10 migration, only tests/unit/core/test_dual_auth.py should reference it; that file is deleted in this commit.)
    - app/core/auth.py (BearerAuthMiddleware) — verify no imports outside main.py
    - app/core/feature_flags.py — extract is_hcaptcha_enabled before deleting is_auth_v2_enabled
    - tests/unit/core/test_dual_auth.py (verify it tests DualAuthMiddleware specifically; if it has portions covering surviving behavior, route those into Plan 10's integration migration BEFORE deleting — but since DualAuth is structurally deleted, all behavioral coverage of the new authenticated_user dep lives in Plan 04's tests/integration/test_authenticated_user_dep.py)
    - tests/unit/core/test_container.py (verify it tests dependency_injector Container resolution; this code is deleted in Plan 12)
    - 19-PATTERNS.md §"app/main.py" (lines 526-580)
  </read_first>
  <files>app/main.py, app/core/dual_auth.py, app/core/auth.py, app/core/feature_flags.py, tests/unit/core/test_dual_auth.py, tests/unit/core/test_container.py</files>
  <action>
    Pre-flight check: run `grep -rn "DualAuthMiddleware\|BearerAuthMiddleware" tests/integration/` and confirm ZERO matches (Plan 10 migrated these). If any matches found, ABORT — Plan 10 is incomplete. Do not proceed.

    Six surgical changes (one atomic commit):

    1. **app/main.py imports cleanup** (lines 45-49, 61) — DELETE these lines:
       ```python
       from app.core.auth import BearerAuthMiddleware
       from app.core.container import Container  # keep until Plan 13; move deletion there
       from app.core.dual_auth import DualAuthMiddleware
       from app.core.feature_flags import is_auth_v2_enabled
       ```
       (CsrfMiddleware import stays until Plan 12. Container import stays until Plan 13.)

    2. **app/main.py middleware stack** (lines ~198-208) — replace the if/else block:
       ```python
       # BEFORE:
       if is_auth_v2_enabled():
           app.add_middleware(CsrfMiddleware, container=container)
           app.add_middleware(DualAuthMiddleware, container=container)
       else:
           app.add_middleware(BearerAuthMiddleware)

       # AFTER:
       app.add_middleware(CsrfMiddleware, container=container)
       # DualAuthMiddleware DELETED — auth lives in Depends(authenticated_user)
       # BearerAuthMiddleware DELETED — V2 is THE auth path
       ```
       Keep CsrfMiddleware temporarily — Plan 12 removes it after verifier confirms ALL state-mutating cookie-auth routes have Depends(csrf_protected).

    3. **app/main.py router registration + prod guard** (lines ~247-262) — replace:
       ```python
       # BEFORE:
       if is_auth_v2_enabled():
           app.include_router(auth_router)
           app.include_router(key_router)
           app.include_router(account_router)
           app.include_router(billing_router)
           app.include_router(ws_ticket_router)

       if settings.ENVIRONMENT == "production" and not is_auth_v2_enabled():
           raise RuntimeError(...)

       # AFTER:
       app.include_router(auth_router)
       app.include_router(key_router)
       app.include_router(account_router)
       app.include_router(billing_router)
       app.include_router(ws_ticket_router)
       ```
       Production guard DELETED entirely.

    4. **DELETE app files**:
       - `git rm app/core/dual_auth.py`
       - `git rm app/core/auth.py`

    5. **app/core/feature_flags.py** — REMOVE the `is_auth_v2_enabled` function. KEEP `is_hcaptcha_enabled` (Phase 13-01 lock). If the module ends up with only one function, leave it; do NOT delete the file.

    6. **DELETE unit test files** (they test code deleted in steps 4 + Plan 13):
       - `git rm tests/unit/core/test_dual_auth.py`
       - `git rm tests/unit/core/test_container.py`
       Document deletion in commit message: "DELETE test_dual_auth.py + test_container.py — tested infrastructure that this phase replaced. Behavioral coverage shifted to tests/integration/test_authenticated_user_dep.py (Plan 04) + test_csrf_protected_dep.py (Plan 05)."

    Tiger-style:
    - Surgical deletes, no reformatting of unrelated code
    - One commit covers all six changes since they're a coordinated "delete the V2 flag world"
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/ -x --tb=short && grep -rn "DualAuthMiddleware\|BearerAuthMiddleware\|AUTH_V2_ENABLED\|is_auth_v2_enabled" app/ | wc -l | awk '{exit ($1 != 0)}' && test ! -f app/core/dual_auth.py && test ! -f app/core/auth.py && test ! -f tests/unit/core/test_dual_auth.py && test ! -f tests/unit/core/test_container.py</automated>
  </verify>
  <acceptance_criteria>
    - `app/core/dual_auth.py` does NOT exist (file deleted)
    - `app/core/auth.py` does NOT exist (file deleted)
    - `tests/unit/core/test_dual_auth.py` does NOT exist
    - `tests/unit/core/test_container.py` does NOT exist
    - `grep -rn "DualAuthMiddleware\|BearerAuthMiddleware\|AUTH_V2_ENABLED\|is_auth_v2_enabled" app/` returns ZERO matches
    - `grep -c "is_hcaptcha_enabled" app/core/feature_flags.py` == 1 (preserved)
    - `grep -c "if is_auth_v2_enabled" app/main.py` == 0
    - Full backend pytest suite GREEN — every route still resolves auth via Depends(authenticated_user)
    - Pre-flight pass: `grep -rn "DualAuthMiddleware\|BearerAuthMiddleware" tests/integration/` returns ZERO matches (Plan 10 fixture migration complete)
  </acceptance_criteria>
  <done>
    - DualAuth + BearerAuth modules DELETED
    - test_dual_auth.py + test_container.py DELETED
    - AUTH_V2 flag + helper + prod guard removed
    - One commit: `refactor(19-11): delete DualAuthMiddleware + BearerAuthMiddleware + AUTH_V2_ENABLED flag + obsolete unit tests`
  </done>
</task>

</tasks>

<verification>
- All grep counts match
- Files deleted from filesystem (3 in app/, 2 in tests/unit/core/)
- Backend suite green
</verification>

<success_criteria>
- D2 + D3 invariants enforced structurally (files gone, flag gone)
- One atomic commit; suite green per atomic-commit invariant
</success_criteria>

<output>
Create `.planning/phases/19-auth-di-refactor/19-11-SUMMARY.md` after completion.
</output>
