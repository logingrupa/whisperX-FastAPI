---
phase: 19
plan: 13
slug: delete-container-and-dependency-injector
type: execute
wave: 8
depends_on: [11, 12]
files_modified:
  - app/core/container.py
  - app/main.py
  - app/api/dependencies.py
  - app/api/auth_routes.py
  - app/api/key_routes.py
  - app/api/billing_routes.py
  - app/api/billing_webhook_router.py
  - app/api/account_routes.py
  - app/api/task_api.py
  - app/api/ws_ticket_routes.py
  - pyproject.toml
  - uv.lock
autonomous: true
requirements:
  - REFACTOR-01
  - REFACTOR-05
must_haves:
  truths:
    - "app/core/container.py is DELETED."
    - "app/main.py contains zero references to Container or set_container."
    - "app/api/dependencies.py contains zero references to _container, set_container, Container."
    - "All `_v2` suffixes dropped from helpers in dependencies.py AND from imports in all 6 router files (auth/key/billing/billing_webhook/account/task/ws_ticket)."
    - "pyproject.toml has dependency-injector removed from [project] dependencies."
    - "uv.lock regenerated to drop dependency-injector + transitive deps."
    - "grep -rn '_container\\.' app/ → 0"
    - "grep -rn '_v2' app/ → 0"
    - "grep -rn 'dependency_injector' app/ → 0"
  artifacts:
    - path: "app/core/container.py"
      provides: "DELETED"
    - path: "pyproject.toml"
      provides: "dependency-injector line removed"
    - path: "uv.lock"
      provides: "Regenerated lockfile"
    - path: "app/api/dependencies.py"
      provides: "Single namespace — no _v2 suffixes"
    - path: "app/api/auth_routes.py, app/api/key_routes.py, app/api/billing_routes.py, app/api/billing_webhook_router.py, app/api/account_routes.py, app/api/task_api.py, app/api/ws_ticket_routes.py"
      provides: "Imports updated to non-suffixed helper names"
  key_links:
    - from: "Plan 13 grep gates"
      to: "REFACTOR-01 + REFACTOR-05 verification"
      via: "structural invariants enforced via greppability"
      pattern: "_container\\."
---

<objective>
T-19-13 — LAST structural step. After this commit, the dependency_injector library is gone from the codebase + pyproject. All `_container.X()` callsites are eliminated. app/core/container.py file gone. The `_v2` namespace suffix introduced in Plans 03-05 (used to coexist with the legacy helpers) is dropped — there is now a single canonical name per dep, propagated to all 6 router files. Plan 14 adds the no-leak regression test.

Purpose: D1 final closure — REFACTOR-01 + REFACTOR-05 verifier gates close.

Output: 1 file DELETED + 11 files edited (main.py, dependencies.py, 7 routers, pyproject.toml, uv.lock).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth-di-refactor/19-CONTEXT.md
@.planning/phases/19-auth-di-refactor/19-PATTERNS.md
@app/core/container.py
@app/main.py
@app/api/dependencies.py
@app/api/auth_routes.py
@app/api/key_routes.py
@app/api/billing_routes.py
@app/api/billing_webhook_router.py
@app/api/account_routes.py
@app/api/task_api.py
@app/api/ws_ticket_routes.py
@pyproject.toml
</context>

<interfaces>
Pre-conditions verified at this wave:
- Plan 07 done — every HTTP router uses Depends chain (with `_v2` suffix in import statements)
- Plan 08 done — WS uses SessionLocal context-manager
- Plan 09 done — background task uses SessionLocal context-manager
- Plan 10 done — integration test fixtures migrated to dependency_overrides[get_db]
- Plan 11 done — DualAuth + BearerAuth + AUTH_V2 deleted (+ unit tests)
- Plan 12 done — CsrfMiddleware deleted (+ unit test)

Now `_container.X()` is referenced ONLY by app/api/dependencies.py legacy helpers and by app/main.py Container() instantiation. This plan deletes both AND drops the `_v2` namespace suffix.

Helpers in app/api/dependencies.py touched by the rename (Plans 03-05):
- get_user_repository_v2 → get_user_repository
- get_api_key_repository_v2 → get_api_key_repository
- get_task_repository_v2 → get_task_repository
- get_rate_limit_repository_v2 → get_rate_limit_repository
- get_auth_service_v2 → get_auth_service
- get_key_service_v2 → get_key_service
- get_account_service_v2 → get_account_service
- get_rate_limit_service_v2 → get_rate_limit_service
- get_free_tier_gate_v2 → get_free_tier_gate
- get_usage_event_writer_v2 → get_usage_event_writer
- get_scoped_task_repository_v2 → get_scoped_task_repository
- get_task_management_service_v2 → get_task_management_service

Each rename propagates to the importer routers.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy _container helpers from app/api/dependencies.py + drop _v2 suffix everywhere</name>
  <read_first>
    - app/api/dependencies.py (full file — focus on functions that still reference `_container`)
    - Verify which legacy functions are still imported anywhere via `grep -rn "from app.api.dependencies import" app/ tests/`
    - Verify all 7 router files for current `_v2` import statements: `grep -rn "_v2" app/api/`
  </read_first>
  <files>app/api/dependencies.py, app/api/auth_routes.py, app/api/key_routes.py, app/api/billing_routes.py, app/api/billing_webhook_router.py, app/api/account_routes.py, app/api/task_api.py, app/api/ws_ticket_routes.py</files>
  <action>
    Identify the LEGACY helpers in dependencies.py that still call `_container.X()` (the 16 callsites enumerated in 19-CONTEXT.md `## Files in scope`):
    - get_task_repository (legacy, lines 38-57)
    - get_file_service (lines 60-72)
    - get_task_management_service (legacy)
    - get_authenticated_user (legacy — Plan 04 added authenticated_user)
    - get_csrf_service (legacy — Plan 02 services.get_csrf_service replaces)
    - get_key_service (legacy)
    - get_auth_service (legacy)
    - get_rate_limit_service (legacy)
    - get_account_service / get_db_session (legacy)
    - get_scoped_task_repository (legacy)
    - get_scoped_task_management_service (legacy)
    - set_container helper
    - the `_container` global itself
    - the `Container | None` type import

    ALL of these are now superseded by Plan 03 / 04 / 05 _v2 functions. Delete them.

    Action steps:
    1. `grep -rn "from app.api.dependencies import" app/ tests/` to identify any remaining import of the legacy names. Code in `app/` MUST be 100% migrated already (Plans 06-09); any test imports of legacy names are a Plan 10 fixture-migration regression — abort if found.
    2. Delete legacy helpers + the global `_container` + `set_container` + `from app.core.container import Container` import line.
    3. RENAME the _v2 helpers to drop the suffix (now that legacy is gone there's no namespace conflict). For each helper:
       - dependencies.py: `def get_user_repository_v2(...)` → `def get_user_repository(...)`
       - dependencies.py: every internal call site using the `_v2` suffix is updated
    4. Propagate the rename to ALL 7 router files (auth_routes, key_routes, billing_routes, billing_webhook_router, account_routes, task_api, ws_ticket_routes):
       - `from app.api.dependencies import authenticated_user, csrf_protected, get_auth_service_v2`
       - becomes: `from app.api.dependencies import authenticated_user, csrf_protected, get_auth_service`
       - And every `Depends(get_*_v2)` → `Depends(get_*)`

    Verify after edits: `grep -rn "_v2" app/` returns ZERO matches.

    Tiger-style:
    - Single-purpose deletion + rename
    - No new logic
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/ -x --tb=short && grep -c "_container" app/api/dependencies.py | grep -q 0 && grep -c "set_container" app/api/dependencies.py | grep -q 0 && grep -c "from app.core.container" app/api/dependencies.py | grep -q 0 && grep -rn "_v2" app/ | wc -l | awk '{exit ($1 != 0)}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "_container" app/api/dependencies.py` returns ZERO matches
    - `grep -rn "set_container" app/` returns ZERO matches
    - `grep -rn "from app.core.container" app/api/` returns ZERO matches
    - `grep -rn "_v2" app/` returns ZERO matches (single namespace; no suffixes anywhere)
    - All 7 router files (auth_routes, key_routes, billing_routes, billing_webhook_router, account_routes, task_api, ws_ticket_routes) updated to import the new (no-suffix) names
    - Full backend pytest suite GREEN
  </acceptance_criteria>
  <done>Legacy helpers gone; namespace de-suffixed; all routers updated.</done>
</task>

<task type="auto">
  <name>Task 2: Delete app/core/container.py + main.py Container() lines + pyproject dependency-injector entry + uv.lock regen</name>
  <read_first>
    - app/main.py (focus on lines 73-80 — Container() instantiation block)
    - app/core/container.py (verify nothing else imports it after Task 1 edit)
    - pyproject.toml (find `"dependency-injector>=4.41.0"` in [project] dependencies)
  </read_first>
  <files>app/core/container.py, app/main.py, pyproject.toml, uv.lock</files>
  <action>
    Five sequential changes:

    1. **app/main.py — delete Container() block** (current lines 73-80):
       ```python
       # DELETE these lines:
       container = Container()
       from app.api import dependencies  # noqa: E402
       dependencies.set_container(container)
       ```
       Also drop the `from app.core.container import Container` import (currently at line 47).

    2. **DELETE file**: `git rm app/core/container.py`.

    3. **pyproject.toml** — remove the line `"dependency-injector>=4.41.0",` from `[project] dependencies` (at ~line 31).

    4. **Regenerate lockfile**: `.venv/Scripts/python.exe -m uv lock` (or `uv lock` if PATH includes it). Stage `uv.lock`. The transitive `six`, `dependency-injector-stubs`, etc. should drop out automatically.

    5. Verify importability: `.venv/Scripts/python.exe -c "import app.main"` must succeed (no ModuleNotFoundError for dependency_injector).

    Tiger-style:
    - File deletion is single-purpose
    - main.py edit is two-line removal
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/ -x --tb=short && grep -rn "_container\\.\\|dependency_injector\\|from app.core.container" app/ | wc -l | awk '{exit ($1 != 0)}' && test ! -f app/core/container.py && grep -c "dependency-injector" pyproject.toml | grep -q 0 && .venv/Scripts/python.exe -c "import app.main"</automated>
  </verify>
  <acceptance_criteria>
    - `app/core/container.py` does NOT exist
    - `grep -rn "_container\." app/` returns ZERO matches (G1 — verifier gate from VALIDATION.md)
    - `grep -rn "dependency_injector" app/` returns ZERO matches (G3)
    - `grep -c "dependency-injector" pyproject.toml` == 0
    - `uv.lock` regenerated and committed
    - `.venv/Scripts/python.exe -c "import app.main"` succeeds without ImportError
    - Full backend pytest suite GREEN
  </acceptance_criteria>
  <done>
    - Container module + dependency_injector library structurally gone
    - One commit covering all 4 files: `refactor(19-13): delete app/core/container.py + drop dependency-injector dependency`
    - REFACTOR-01 + REFACTOR-05 invariants now structurally enforced
  </done>
</task>

</tasks>

<verification>
- All grep gates G1, G3 from VALIDATION.md hit ZERO
- `_v2` suffix gone everywhere
- pyproject.toml + uv.lock match (no dependency_injector residue)
- App boots; tests green
</verification>

<success_criteria>
- D1 final closure
- One atomic commit per task (Task 1 = legacy helper deletion + rename across 8 files; Task 2 = container.py + pyproject + lockfile + main.py) — total 2 commits in this plan
</success_criteria>

<output>
Create `.planning/phases/19-auth-di-refactor/19-13-SUMMARY.md` after completion.
</output>
