---
phase: 17
plan: 03
type: execute
wave: 3
depends_on: [17-01, 17-02]
files_modified:
  - README.md
autonomous: true
requirements: [OPS-05]
tags: [docs, readme, auth-flow, ops]
must_haves:
  truths:
    - "README.md has a new top-level `## Authentication & API Keys (v1.2)` section"
    - "The Auth section is inserted AFTER the Web UI (v1.0) block AND BEFORE the Tech Stack subsection / next major section"
    - "Five required subheadings present: Registration & Login Flow, Issuing API Keys, Using an API Key, Free vs Pro Tiers, Migrating from v1.1"
    - "Free vs Pro markdown table with 5 limit rows (Requests/hour, File size, Duration cap, Models, Concurrent slots)"
    - "Curl snippets present for: register-then-issue-key (cookie session) AND transcribe via bearer (whsk_*)"
    - "Migrating from v1.1 subsection links to docs/migration-v1.2.md (no migration step bodies duplicated)"
    - "mailto:hey@logingrupa.lv password reset path is documented"
  artifacts:
    - path: "README.md"
      provides: "OPS-05 — auth flow + key issuance + bearer usage + tier matrix + reset path documented"
      contains: "## Authentication & API Keys (v1.2)"
      contains: "### Registration & Login Flow"
      contains: "### Issuing API Keys"
      contains: "### Using an API Key"
      contains: "### Free vs Pro Tiers"
      contains: "### Migrating from v1.1"
      contains: "whsk_"
      contains: "mailto:hey@logingrupa.lv"
      contains: "Authorization: Bearer whsk_"
      contains: "docs/migration-v1.2.md"
  key_links:
    - from: "README.md `### Migrating from v1.1`"
      to: "docs/migration-v1.2.md"
      via: "relative-link reference; runbook owns the step bodies (DRY)"
      pattern: "\\[.*\\]\\(docs/migration-v1\\.2\\.md\\)"
    - from: "README.md `### Issuing API Keys` / `### Using an API Key`"
      to: "POST /api/keys + Authorization: Bearer whsk_<prefix>_<random>"
      via: "curl snippets matching Phase 13-04 contract verbatim"
      pattern: "Authorization: Bearer whsk_"
    - from: "README.md `### Registration & Login Flow`"
      to: "mailto:hey@logingrupa.lv password reset path"
      via: "AUTH-07 — manual operator response, no SMTP"
      pattern: "mailto:hey@logingrupa\\.lv"
---

<objective>
Insert a new `## Authentication & API Keys (v1.2)` section into README.md delivering OPS-05. Five anchored subheadings: Registration & Login Flow, Issuing API Keys, Using an API Key, Free vs Pro Tiers, Migrating from v1.1. Three curl snippets (register-set-cookie, list-keys-via-cookie, transcribe-via-bearer). Free vs Pro markdown table. mailto password reset link. Migration subsection LINKS to `docs/migration-v1.2.md` — does NOT duplicate migration step bodies (DRY enforcement).

Purpose: An external API consumer reads README and understands how to register, issue keys, transcribe via bearer, and what the free/pro limits are. Phase 17 success criterion 3 verbatim.
Output: README.md modified in place (~80 new lines inserted between Web UI block and Tech Stack subsection).

SRP: this section is API-consumer entrypoint only — registration + key usage + tier matrix + reset path. NO migration step bodies (those live in `docs/migration-v1.2.md`). NO env-var schema (that lives in `.env.example`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/17-docs-migration-runbook-operator-guide/17-CONTEXT.md
@.planning/phases/17-docs-migration-runbook-operator-guide/17-01-PLAN.md
@.planning/phases/17-docs-migration-runbook-operator-guide/17-02-PLAN.md

@README.md
@docs/migration-v1.2.md
@.env.example

<interfaces>
<!-- Existing README structure (verbatim — preserve order) -->
# whisperX REST API
(badges)
---
## Fork Information
---
## Web UI (v1.0)
### Features
### Tech Stack            <-- a SUBSECTION inside Web UI block
### Frontend Installation
### Architecture
### How It Works
### Configuration
---
(blank-line + horizontal rule, then top-level prose continues)
The whisperX API is a tool for...
## Documentation
### Language and Whisper model settings
### Supported File Formats
### Available Services
### Task management and result storage
### Compute Settings
### Available Models
## System Requirements
## Getting Started
### Local Run
### Logging Configuration
### Docker Build
## Troubleshooting
...

<!-- INSERTION POINT: AFTER the closing of `## Web UI (v1.0)` block (after `### Configuration`
     subsection content + the trailing `---` separator), BEFORE the line
     `The whisperX API is a tool for enhancing and analyzing audio content.` -->
<!-- This places the new top-level Auth section between the Web UI block and the
     prose-level introduction of the API itself. -->

<!-- Free vs Pro values (locked from REQUIREMENTS.md + PROJECT.md decisions) -->
| Limit             | Free                       | Pro (€5/mo, v1.3 stub)                |
|-------------------|----------------------------|---------------------------------------|
| Requests / hour   | 5                          | 100                                   |
| File size         | <= 5 min audio             | <= 60 min audio                       |
| Duration cap      | 30 min audio per day       | 600 min audio per day                 |
| Models            | tiny, small                | all (incl. large-v3)                  |
| Concurrent slots  | 1 (back-of-queue)          | 3 (queue priority)                    |

<!-- API key shape (from Phase 11 KEY-02) -->
whsk_<8-char-prefix>_<22-char-base64-random>
Total length: 36 chars. ~128 bits entropy. Header form: Authorization: Bearer whsk_<prefix>_<random>.

<!-- Routes (from Phase 13-03 / 13-04 / 13-05) -->
POST /auth/register      {email, password}              -> 201 + httpOnly cookie session
POST /auth/login         {email, password}              -> 200 + httpOnly cookie session
POST /auth/logout                                       -> 204 + Set-Cookie: clear
POST /api/keys           {name}  (cookie + X-CSRF-Token) -> 201 + {key: "whsk_..."} (show-once)
GET  /api/keys           (cookie OR bearer)             -> 200 + [{name,prefix,created_at,last_used_at,status}]
DELETE /api/keys/{id}    (cookie + X-CSRF-Token)        -> 204 (soft-delete, audit-preserved)
POST /speech-to-text     (cookie OR bearer)             -> 202 + {task_id}
GET  /task/{task_id}     (cookie OR bearer)             -> 200 + {status, result}

<!-- Password reset (AUTH-07) -->
No SMTP in v1.2. Login page exposes a mailto:hey@logingrupa.lv link; user emails the operator.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README curl snippets -> external API consumer | snippets MUST work as-shown against a deployed v1.2 instance — drift between docs and Phase 13-04 contract = silent breakage |
| README -> docs/migration-v1.2.md | DRY enforcement: migration step bodies live in the runbook only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 | Tampering | curl snippet shows wrong header form (e.g., `Authorization: whsk_...` without `Bearer`) | mitigate | Snippet uses literal `Authorization: Bearer whsk_<prefix>_<random>` matching Phase 13-04 contract; verifier-grep asserts the literal string |
| T-17-09 | Information Disclosure | README exposes a real-looking API key | mitigate | Snippets use the placeholder form `whsk_PREFIX_<22charrandom>` with the literal token "PREFIX" — clearly not a real key |
| T-17-10 | Repudiation | README documents a different password-reset address than the production deployment honors | mitigate | mailto link `mailto:hey@logingrupa.lv` matches PROJECT.md and `app/api/auth_routes.py` 403 detail (locked invariant) |
| T-17-11 | Denial-of-Service | docs and runbook drift over time; operator follows README migration steps and runbook differs | mitigate | README's `### Migrating from v1.1` subsection contains NO step bodies — it is purely a link to `docs/migration-v1.2.md`; cross-file DRY verified by acceptance grep |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Insert `## Authentication & API Keys (v1.2)` section into README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (full current file — find exact insertion anchor)
    - .planning/phases/17-docs-migration-runbook-operator-guide/17-CONTEXT.md (decisions block — five-subsection layout)
    - .planning/REQUIREMENTS.md (RATE-03..RATE-10 for tier values; AUTH-07 for mailto path; KEY-02 for whsk_ format)
    - docs/migration-v1.2.md (link target — confirms the file path for the Migrating from v1.1 link)
  </read_first>
  <action>
Read the current `README.md` to find the exact insertion anchor.

Insertion strategy: locate the boundary between the `## Web UI (v1.0)` block and the prose paragraph `The whisperX API is a tool for enhancing and analyzing audio content. This API provides a suite of services for processing audio and video files...`. The `---` horizontal rule between them is the anchor.

Use Edit (not Write) to splice in the new section. The pattern: find the line `In production, the frontend is served directly by FastAPI at `/ui`, so no proxy is needed.` (last line of `### Configuration` subsection) followed by a blank line, then `---`, then a blank line, then `The whisperX API is a tool for enhancing and analyzing audio content.`.

Insert the new `## Authentication & API Keys (v1.2)` block AFTER the `---` rule and BEFORE `The whisperX API is a tool...`. Add a trailing `---` after the new section so the visual separator pattern matches the rest of the document.

Exact block to insert (verbatim — do NOT paraphrase headings, table values, or curl snippets):

```markdown
## Authentication & API Keys (v1.2)

Multi-user authentication and personal API keys ship in v1.2. The browser UI uses an httpOnly cookie session; external API consumers send a raw bearer key. One middleware (`DualAuthMiddleware`) accepts both.

### Registration & Login Flow

```
+------------------+      POST /auth/register       +------------------+
| Browser /ui      |  --------------------------->  | DualAuthMiddleware|
| (cookie session) |  <----- Set-Cookie: session    |                  |
+------------------+                                 +------------------+
       |                                                      |
       |  POST /auth/login (existing user)                    |
       |  ---------------------------------------------->     |
       |  <----- 200 + Set-Cookie: session + csrf_token ----- |
       |                                                      |
       |  POST /auth/logout                                   |
       |  ---------------------------------------------->     |
       |  <----- 204 + Set-Cookie: clear --------------------- |
```

- **Register**: `POST /auth/register {email, password}` -> 201 + httpOnly + Secure + SameSite=Lax session cookie
- **Login**: `POST /auth/login {email, password}` -> 200 + httpOnly session cookie + non-httpOnly `csrf_token` cookie
- **Logout**: `POST /auth/logout` (cookie + matching `X-CSRF-Token` header) -> 204 + cleared cookies
- **Logout all devices**: `POST /auth/logout-all` -> bumps `users.token_version`, invalidating every prior JWT for that user (AUTH-06)
- **Rate limits**: 3 register / hr / IP-/24 (ANTI-01), 10 login / hr / IP-/24 (ANTI-02)
- **Disposable email registrations are rejected** with a generic error (ANTI-04)
- **Password reset**: no SMTP in v1.2. Click the [mailto:hey@logingrupa.lv](mailto:hey@logingrupa.lv) link on the login page and the operator responds manually (AUTH-07)

### Issuing API Keys

Authenticated browser users issue keys via `/dashboard/keys`. External clients can issue keys over the cookie session via curl:

```bash
# 1. Log in to get the cookie session.
curl -c cookies.txt -b cookies.txt \
  -X POST https://your-host/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"you@example.com","password":"your-password"}'

# 2. Read the csrf_token cookie value.
CSRF=$(awk '$6=="csrf_token"{print $7}' cookies.txt)

# 3. Issue a key. The plaintext is returned EXACTLY ONCE — copy it now.
curl -c cookies.txt -b cookies.txt \
  -X POST https://your-host/api/keys \
  -H "Content-Type: application/json" \
  -H "X-CSRF-Token: $CSRF" \
  -d '{"name":"my-laptop"}'
# Response: {"id": 1, "name": "my-laptop", "prefix": "PREFIX01",
#            "key": "whsk_PREFIX01_<22charbase64random>", "created_at": "..."}
```

- Key format: `whsk_<8-char-prefix>_<22-char-base64-random>` (~128 bits entropy, 36 chars total)
- The full plaintext is shown EXACTLY ONCE in the response body. After that, only the prefix is visible (KEY-04)
- Multiple active keys per user are allowed (KEY-06). Revoke via `DELETE /api/keys/{id}` (soft-delete, audit-preserved)

### Using an API Key

External clients send the bearer key in the `Authorization` header. Bearer-authenticated requests skip CSRF entirely (MID-04).

```bash
# Submit a transcription job via bearer.
curl -X POST https://your-host/speech-to-text \
  -H "Authorization: Bearer whsk_PREFIX01_<22charbase64random>" \
  -F "file=@/path/to/audio.wav" \
  -F "language=en" \
  -F "model=tiny"
# Response: 202 Accepted + {"task_id": "<uuid>"}

# Poll the task.
curl https://your-host/task/<uuid> \
  -H "Authorization: Bearer whsk_PREFIX01_<22charbase64random>"
# Response: {"status": "completed", "result": {...}}
```

`whsk_*` keys work on every authenticated route. WebSocket connections require a 60-second single-use ticket from `POST /api/ws/ticket` (MID-06) — see the OpenAPI spec at `/openapi.json` for the WS handshake.

### Free vs Pro Tiers

Default tier is `trial`. The trial counter starts at first key creation (RATE-08). After expiry the user lands on `free`.

| Limit | Free | Pro (€5/mo, v1.3 stub) |
|-------|------|-------------------------|
| Requests / hour | 5 | 100 |
| File size | <= 5 min audio | <= 60 min audio |
| Duration cap | 30 min audio per day | 600 min audio per day |
| Models | `tiny`, `small` | all (incl. `large-v3`) |
| Concurrent slots | 1 (back-of-queue) | 3 (queue priority) |

Limit hits return:
- `429 Too Many Requests` with `Retry-After` header (RATE-12) when the per-hour cap is exceeded
- `402 Payment Required` after trial expiry on transcription routes (RATE-09)
- `403 Forbidden` when a free-tier user requests a Pro-only model or diarization (RATE-06)

Pro checkout (`POST /billing/checkout`) and the Stripe webhook (`POST /billing/webhook`) return `501 Not Implemented` in v1.2 — schema is in place; live Stripe ships in v1.3 (BILL-05, BILL-06).

### Migrating from v1.1

If you are upgrading an existing v1.1 deployment (single shared `API_BEARER_TOKEN`, no users table), follow the runbook:

[docs/migration-v1.2.md](docs/migration-v1.2.md)

The runbook covers backup, alembic stamp + upgrade sequence, admin-user creation, task backfill, and rollback. Step ordering mirrors `tests/integration/test_migration_smoke.py` (VERIFY-08, executable proof-of-runbook).

For new deployments, copy `.env.example` to `.env`, fill in the v1.2 secrets (`JWT_SECRET`, `CSRF_SECRET` — generate with `openssl rand -hex 32`), and run `alembic upgrade head` against an empty DB.

---
```

Insertion mechanics:
- Read README.md fully first to confirm the exact byte sequence of the boundary (Edit requires unique old_string).
- Identify the boundary as: the `### Configuration` subsection's last line + blank line + `---` + blank line + the line `The whisperX API is a tool for enhancing and analyzing audio content. This API provides a suite of services for processing audio and video files, including transcription, alignment, diarization, and combining transcript with diarization results.`
- old_string: 3 lines starting at `In production, the frontend is served directly by FastAPI at \`/ui\`, so no proxy is needed.` and ending at `The whisperX API is a tool for enhancing and analyzing audio content. This API provides a suite of services for processing audio and video files, including transcription, alignment, diarization, and combining transcript with diarization results.`
- new_string: same lead-in 3 lines + the entire `## Authentication & API Keys (v1.2)` block above (with its trailing `---`) + same closing line.
- DO NOT use Write — preserve the rest of README.md byte-for-byte.

DRY enforcement (verifier-grep):
- The README does NOT contain `alembic stamp 0001_baseline` (that lives in the runbook only).
- The README does NOT contain `python -m app.cli backfill-tasks` (that lives in the runbook only).
- The README's `### Migrating from v1.1` subsection contains EXACTLY ONE link to `docs/migration-v1.2.md` and NO migration step bodies.

SRP enforcement:
- The new section contains API-consumer guidance only — registration, key issuance, bearer usage, tier matrix, reset path, migration link.
- It does NOT contain env-var schema (that lives in `.env.example`).
- It does NOT contain migration step bodies (that lives in the runbook).

Tiger-style:
- Every curl snippet shows the literal `Authorization: Bearer whsk_PREFIX01_<22charbase64random>` form — verifier-grep asserts the exact header.
- Every route is shown with the HTTP method and path verbatim (`POST /auth/register`, not "the register endpoint").
- The free/pro table cell values are exact (`5`, `100`, `<= 5 min audio`, etc.) — no "approximately" or "around".

Self-explanatory:
- Subheadings name the action (`### Issuing API Keys`), not the abstraction (`### Key Lifecycle`).
- Curl variables are named (`CSRF`, not `T`); placeholders use literal `PREFIX01` (clearly fake).
  </action>
  <verify>
    <automated>test -f README.md && grep -c "## Authentication & API Keys (v1.2)" README.md</automated>
  </verify>
  <done>
    - README.md contains the new `## Authentication & API Keys (v1.2)` top-level section
    - Five subheadings present in locked order
    - Curl snippets for register-then-issue-key (cookie) and transcribe-via-bearer (whsk_*)
    - Free vs Pro markdown table with 5 limit rows
    - mailto:hey@logingrupa.lv password reset link present
    - Migrating from v1.1 subsection links to docs/migration-v1.2.md
    - No migration step bodies duplicated from runbook (DRY)
  </done>
  <acceptance_criteria>
    - `test -f README.md` exits 0
    - `grep -c "^## Authentication & API Keys (v1.2)$" README.md` == 1
    - `grep -c "^### Registration & Login Flow$" README.md` == 1
    - `grep -c "^### Issuing API Keys$" README.md` == 1
    - `grep -c "^### Using an API Key$" README.md` == 1
    - `grep -c "^### Free vs Pro Tiers$" README.md` == 1
    - `grep -c "^### Migrating from v1.1$" README.md` == 1
    - `grep -c "Authorization: Bearer whsk_" README.md` >= 2
    - `grep -c "whsk_PREFIX01" README.md` >= 2
    - `grep -c "mailto:hey@logingrupa.lv" README.md` >= 1
    - `grep -c "docs/migration-v1.2.md" README.md` >= 1
    - `grep -c "POST /auth/register" README.md` >= 1
    - `grep -c "POST /auth/login" README.md` >= 1
    - `grep -c "POST /auth/logout" README.md` >= 1
    - `grep -c "POST /api/keys" README.md` >= 1
    - `grep -c "POST /speech-to-text" README.md` >= 1
    - Free vs Pro table rows: `grep -c "^| Requests / hour" README.md` == 1; `grep -c "^| File size" README.md` == 1; `grep -c "^| Duration cap" README.md` == 1; `grep -c "^| Models" README.md` == 1; `grep -c "^| Concurrent slots" README.md` == 1
    - Subsection ordering: lines for headings monotonically increase. Verify with: `awk '/^### Registration & Login Flow$/{a=NR} /^### Issuing API Keys$/{b=NR} /^### Using an API Key$/{c=NR} /^### Free vs Pro Tiers$/{d=NR} /^### Migrating from v1.1$/{e=NR} END{exit !(a<b && b<c && c<d && d<e)}' README.md` exits 0
    - Cross-file DRY: `grep -c "alembic stamp 0001_baseline" README.md` == 0
    - Cross-file DRY: `grep -c "python -m app.cli backfill-tasks" README.md` == 0
    - Cross-file DRY: `grep -c "alembic upgrade 0002_auth_schema" README.md` == 0
    - Pre-existing top-level structure preserved: `grep -c "^## Web UI (v1.0)$" README.md` == 1; `grep -c "^## Documentation$" README.md` == 1; `grep -c "^## Getting Started$" README.md` == 1; `grep -c "^## Troubleshooting$" README.md` == 1
    - Insertion position: line containing `## Authentication & API Keys (v1.2)` is AFTER `## Web UI (v1.0)` AND BEFORE `## Documentation`. Verify with: `awk '/^## Web UI \(v1\.0\)$/{w=NR} /^## Authentication & API Keys \(v1\.2\)$/{a=NR} /^## Documentation$/{d=NR} END{exit !(w<a && a<d)}' README.md` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- README renders correctly when previewed as Markdown (table aligns, code fences close, link resolves)
- The `[docs/migration-v1.2.md](docs/migration-v1.2.md)` relative link resolves on GitHub and locally
- An external API consumer copies a curl snippet, replaces the placeholder host + key, and the call works against a v1.2 deployment
- DRY: cross-file grep proves no migration step body is duplicated from runbook (`alembic stamp 0001_baseline`, `python -m app.cli backfill-tasks` absent)
- SRP: README contains zero env-var assignments (`grep -c "^JWT_SECRET=" README.md` == 0)
</verification>

<success_criteria>
- README.md has new `## Authentication & API Keys (v1.2)` section in correct position (after Web UI, before Documentation)
- Five anchored subheadings present in locked order
- Three curl snippets (login + key issuance, transcribe via bearer, poll task)
- Free vs Pro markdown table with 5 limit rows
- mailto password reset link present
- Migrating from v1.1 subsection links to docs/migration-v1.2.md (no step bodies duplicated)
- Phase 17 success criterion 3 deliverable: README documents registration/login flow, API key issuance and bearer usage, free-vs-Pro tier differences, and the manual mailto password-reset path
</success_criteria>

<output>
After completion, create `.planning/phases/17-docs-migration-runbook-operator-guide/17-03-SUMMARY.md`
</output>
