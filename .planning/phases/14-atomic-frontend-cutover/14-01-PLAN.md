---
phase: 14-atomic-frontend-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vitest.config.ts
  - frontend/vite.config.ts
  - frontend/src/tests/setup.ts
  - frontend/src/tests/msw/handlers.ts
  - frontend/src/tests/msw/auth.handlers.ts
  - frontend/src/tests/msw/keys.handlers.ts
  - frontend/src/tests/msw/ws.handlers.ts
  - frontend/public/mockServiceWorker.js
  - frontend/src/components/ui/form.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/label.tsx
  - frontend/src/components/ui/dialog.tsx
  - frontend/src/components/ui/alert.tsx
  - frontend/.env.example
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-05
  - UI-13
must_haves:
  truths:
    - "bun run test exits 0 on a sentinel passing test"
    - "MSW worker init present in frontend/public/mockServiceWorker.js"
    - "shadcn/ui form/input/label/dialog/alert components installed under @/components/ui"
    - "Vitest jsdom env loads @testing-library/jest-dom matchers globally"
  artifacts:
    - path: frontend/vitest.config.ts
      provides: "Vitest 3.2 config (jsdom env, setupFiles, globals)"
      contains: "environment: 'jsdom'"
    - path: frontend/src/tests/setup.ts
      provides: "Global test setup (jest-dom, MSW server, BroadcastChannel polyfill)"
      contains: "import '@testing-library/jest-dom/vitest'"
    - path: frontend/src/tests/msw/handlers.ts
      provides: "MSW handler barrel re-exporting auth/keys/ws splits"
      exports: ["handlers"]
    - path: frontend/public/mockServiceWorker.js
      provides: "MSW browser worker init (msw init output)"
    - path: frontend/src/components/ui/form.tsx
      provides: "shadcn Form (react-hook-form adapter)"
    - path: frontend/src/components/ui/dialog.tsx
      provides: "shadcn Dialog (Radix)"
    - path: frontend/src/components/ui/alert.tsx
      provides: "shadcn Alert"
  key_links:
    - from: frontend/vitest.config.ts
      to: frontend/src/tests/setup.ts
      via: "setupFiles array entry"
      pattern: "setupFiles.*setup\\.ts"
    - from: frontend/src/tests/setup.ts
      to: frontend/src/tests/msw/handlers.ts
      via: "setupServer(...handlers)"
      pattern: "setupServer\\(\\.\\.\\.handlers\\)"
---

<objective>
Establish frontend test infrastructure (Vitest 3.2 + jsdom + RTL 16 + user-event 14 + jest-dom 6 + MSW 2.13) and install all runtime + dev deps, plus shadcn components, that subsequent plans (apiClient, authStore, pages) consume. This is foundation work — zero behavior change, but every later plan depends on it being present.

Purpose: Single Wave 1 install/configure pass. Plans 02-07 assume vitest + RTL + MSW + shadcn primitives are ready.
Output: Working test runner (`bun run test` passes a sentinel test), MSW worker file, full shadcn component set, all deps locked in package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-atomic-frontend-cutover/14-CONTEXT.md

<interfaces>
<!-- shadcn components.json (already present, locked aliases) -->
- style: "new-york", baseColor: "neutral", cssVariables: true
- aliases: components=@/components, ui=@/components/ui, utils=@/lib/utils, hooks=@/hooks, lib=@/lib

<!-- Existing shadcn components already in repo (do NOT overwrite): -->
frontend/src/components/ui/{badge,button,card,collapsible,progress,scroll-area,select,sonner,tooltip}.tsx

<!-- Existing deps (DO NOT re-add): -->
react@^19.2.0, react-dom@^19.2.0, react-router-dom@^7.13.0, react-use-websocket@^4.13.0,
clsx, class-variance-authority, tailwind-merge, lucide-react, sonner, next-themes,
@radix-ui/react-{collapsible,progress,scroll-area,select,slot,tooltip},
react-dropzone, tus-js-client; vite@^7.2.4, typescript@~5.9.3, @vitejs/plugin-react

<!-- New deps to add (Wave 1 owns these — Plans 02-07 assume present): -->
runtime: zustand@^5, react-hook-form@^7, zod@^3, @hookform/resolvers@^3,
         @radix-ui/react-{dialog,label} (for shadcn dialog/label/form/input/alert)
dev: vitest@^3.2, @vitest/ui@^3.2, jsdom@^25, msw@^2.13,
     @testing-library/react@^16.1, @testing-library/user-event@^14.6,
     @testing-library/jest-dom@^6.6, @types/node@^25 (already present)
</interfaces>

<frontend_design_skill>
Apply `/frontend-design` skill principles when planning visual quality even though this plan only adds primitives. The shadcn components installed here MUST match the "super pro modern UI" bar (UI-13). Use shadcn defaults (new-york style + neutral palette per components.json) — DO NOT customize the generated files.
</frontend_design_skill>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install runtime + dev deps and add shadcn UI components</name>
  <read_first>
    - frontend/package.json (current dep set — see &lt;interfaces&gt; for what already exists)
    - frontend/components.json (shadcn config — DO NOT modify)
  </read_first>
  <files>
    - frontend/package.json
    - frontend/src/components/ui/form.tsx
    - frontend/src/components/ui/input.tsx
    - frontend/src/components/ui/label.tsx
    - frontend/src/components/ui/dialog.tsx
    - frontend/src/components/ui/alert.tsx
  </files>
  <action>
    Add these deps to frontend/package.json (DO NOT remove existing entries):

    dependencies (runtime):
    - "zustand": "^5.0.2"
    - "react-hook-form": "^7.54.2"
    - "zod": "^3.24.1"
    - "@hookform/resolvers": "^3.10.0"
    - "@radix-ui/react-dialog": "^1.1.4"
    - "@radix-ui/react-label": "^2.1.1"

    devDependencies:
    - "vitest": "^3.2.4"
    - "@vitest/ui": "^3.2.4"
    - "jsdom": "^25.0.1"
    - "msw": "^2.13.0"
    - "@testing-library/react": "^16.1.0"
    - "@testing-library/user-event": "^14.6.1"
    - "@testing-library/jest-dom": "^6.6.3"

    Add scripts:
    - "test": "vitest run"
    - "test:watch": "vitest"
    - "test:ui": "vitest --ui"

    Then install: `cd frontend && bun install` (or `npm install`).

    Then add shadcn components — but because shadcn CLI is interactive and may reorder file content, INSTEAD WRITE THESE FILES MANUALLY using the canonical shadcn new-york source. Run via: `cd frontend && npx shadcn@latest add form input label dialog alert -y` if available; if the CLI prompts interactively, fall back to manually writing the 5 files. Reference https://ui.shadcn.com/r/styles/new-york for exact source. Files MUST live under frontend/src/components/ui/ matching aliases in components.json. DO NOT overwrite badge.tsx/button.tsx/card.tsx/collapsible.tsx/progress.tsx/scroll-area.tsx/select.tsx/sonner.tsx/tooltip.tsx — they already exist.

    DRY note (per user code-quality bar §1): the form.tsx file ships shadcn's react-hook-form adapter (`<Form>`, `<FormField>`, `<FormItem>`, `<FormLabel>`, `<FormControl>`, `<FormMessage>`) — do not roll a custom one. Plans 05/06 import from this single source.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; cat package.json | grep -E '"(zustand|react-hook-form|zod|@hookform/resolvers|vitest|jsdom|msw|@testing-library/(react|user-event|jest-dom))"' | wc -l | grep -E '^9$'</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && bun pm ls zustand react-hook-form zod @hookform/resolvers vitest jsdom msw 2>&1 | grep -c "@"` ≥ 7 (deps installed)
    - `ls frontend/src/components/ui/form.tsx frontend/src/components/ui/input.tsx frontend/src/components/ui/label.tsx frontend/src/components/ui/dialog.tsx frontend/src/components/ui/alert.tsx` — all 5 exist
    - `grep -c '"test"' frontend/package.json` ≥ 1 (test script added)
    - `grep -c "FormField" frontend/src/components/ui/form.tsx` ≥ 1 (react-hook-form adapter present)
  </acceptance_criteria>
  <done>All deps installed, all shadcn primitives written, no overwrite of existing components.</done>
</task>

<task type="auto">
  <name>Task 2: Vitest config + tests/setup.ts + MSW scaffolding</name>
  <read_first>
    - frontend/vite.config.ts (existing — extends with test config; see &lt;interfaces&gt;)
    - frontend/tsconfig.app.json (path aliases — `@/*` already mapped)
  </read_first>
  <files>
    - frontend/vitest.config.ts
    - frontend/src/tests/setup.ts
    - frontend/src/tests/msw/handlers.ts
    - frontend/src/tests/msw/auth.handlers.ts
    - frontend/src/tests/msw/keys.handlers.ts
    - frontend/src/tests/msw/ws.handlers.ts
    - frontend/src/tests/__sentinel__.test.ts
    - frontend/public/mockServiceWorker.js
    - frontend/.env.example
  </files>
  <action>
    1) Create `frontend/vitest.config.ts` (separate from vite.config.ts to keep build config minimal). Exact content:

    ```ts
    import { defineConfig } from 'vitest/config';
    import react from '@vitejs/plugin-react';
    import path from 'path';

    export default defineConfig({
      plugins: [react()],
      resolve: {
        alias: { '@': path.resolve(__dirname, './src') },
      },
      test: {
        environment: 'jsdom',
        globals: true,
        setupFiles: ['./src/tests/setup.ts'],
        css: false,
        include: ['src/**/*.test.{ts,tsx}'],
      },
    });
    ```

    2) Create `frontend/src/tests/setup.ts`:

    ```ts
    import '@testing-library/jest-dom/vitest';
    import { cleanup } from '@testing-library/react';
    import { afterAll, afterEach, beforeAll, vi } from 'vitest';
    import { setupServer } from 'msw/node';
    import { handlers } from './msw/handlers';

    // BroadcastChannel polyfill — jsdom 25 lacks BroadcastChannel (CONTEXT §146)
    // Locked decision: vi.stubGlobal with a working implementation backed by
    // an in-memory listener registry so cross-tab sync tests pass deterministically.
    type BcListener = (e: MessageEvent) => void;
    const channels: Map<string, Set<BcListener>> = new Map();
    class MockBroadcastChannel {
      private listeners = new Set<BcListener>();
      constructor(public name: string) {
        if (!channels.has(name)) channels.set(name, new Set());
      }
      postMessage(data: unknown): void {
        const all = channels.get(this.name);
        if (!all) return;
        const event = new MessageEvent('message', { data });
        for (const l of all) {
          if (l !== this.boundListener) l(event);
        }
      }
      private boundListener: BcListener = () => {};
      addEventListener(_t: string, listener: BcListener): void {
        this.listeners.add(listener);
        channels.get(this.name)!.add(listener);
        this.boundListener = listener;
      }
      removeEventListener(_t: string, listener: BcListener): void {
        this.listeners.delete(listener);
        channels.get(this.name)?.delete(listener);
      }
      close(): void {
        for (const l of this.listeners) channels.get(this.name)?.delete(l);
        this.listeners.clear();
      }
    }
    vi.stubGlobal('BroadcastChannel', MockBroadcastChannel);

    // window.location: jsdom provides — but tests that assert redirects need
    // a settable href. Replace with a writable mock.
    const locationMock = {
      href: 'http://localhost/',
      pathname: '/',
      search: '',
      assign: vi.fn(),
      replace: vi.fn(),
    };
    Object.defineProperty(window, 'location', {
      writable: true,
      value: locationMock,
    });

    // navigator.clipboard polyfill for copy-to-clipboard tests
    Object.assign(navigator, {
      clipboard: { writeText: vi.fn().mockResolvedValue(undefined) },
    });

    // MSW Node server (used by Vitest)
    export const server = setupServer(...handlers);
    beforeAll(() => server.listen({ onUnhandledRequest: 'error' }));
    afterEach(() => {
      server.resetHandlers();
      cleanup();
      locationMock.href = 'http://localhost/';
      locationMock.pathname = '/';
      locationMock.search = '';
    });
    afterAll(() => server.close());
    ```

    3) Create `frontend/src/tests/msw/auth.handlers.ts`:

    ```ts
    import { http, HttpResponse } from 'msw';

    export const authHandlers = [
      http.post('/auth/login', async ({ request }) => {
        const body = (await request.json()) as { email: string; password: string };
        if (body.password === 'wrong') {
          return HttpResponse.json(
            { detail: 'Invalid credentials', code: 'INVALID_CREDENTIALS' },
            { status: 401 },
          );
        }
        return HttpResponse.json(
          { user_id: 1, plan_tier: 'trial' },
          {
            status: 200,
            headers: {
              'Set-Cookie': 'session=fake.jwt.token; Path=/; HttpOnly, csrf_token=fake-csrf-123; Path=/',
            },
          },
        );
      }),
      http.post('/auth/register', async () =>
        HttpResponse.json(
          { user_id: 1, plan_tier: 'trial' },
          {
            status: 201,
            headers: {
              'Set-Cookie': 'session=fake.jwt.token; Path=/; HttpOnly, csrf_token=fake-csrf-123; Path=/',
            },
          },
        ),
      ),
      http.post('/auth/logout', () => new HttpResponse(null, { status: 204 })),
    ];
    ```

    4) Create `frontend/src/tests/msw/keys.handlers.ts`:

    ```ts
    import { http, HttpResponse } from 'msw';

    export const keysHandlers = [
      http.get('/api/keys', () =>
        HttpResponse.json([
          {
            id: 1,
            name: 'default',
            prefix: 'whsk_abc1',
            created_at: '2026-04-29T12:00:00Z',
            last_used_at: null,
            status: 'active',
          },
        ]),
      ),
      http.post('/api/keys', async ({ request }) => {
        const body = (await request.json()) as { name: string };
        return HttpResponse.json(
          {
            id: 2,
            name: body.name,
            prefix: 'whsk_xyz2',
            key: 'whsk_xyz2_thisisthe22charplaintext',
            created_at: '2026-04-29T13:00:00Z',
            status: 'active',
          },
          { status: 201 },
        );
      }),
      http.delete('/api/keys/:id', () => new HttpResponse(null, { status: 204 })),
    ];
    ```

    5) Create `frontend/src/tests/msw/ws.handlers.ts`:

    ```ts
    import { http, HttpResponse } from 'msw';

    export const wsHandlers = [
      http.post('/api/ws/ticket', async () =>
        HttpResponse.json(
          { ticket: 'mock-ticket-32chars', expires_at: '2099-01-01T00:00:00Z' },
          { status: 201 },
        ),
      ),
    ];
    ```

    6) Create `frontend/src/tests/msw/handlers.ts` (barrel — DRY per CONTEXT §144 Claude's Discretion: split per resource, recombine here):

    ```ts
    import { authHandlers } from './auth.handlers';
    import { keysHandlers } from './keys.handlers';
    import { wsHandlers } from './ws.handlers';

    export const handlers = [...authHandlers, ...keysHandlers, ...wsHandlers];
    ```

    7) Create `frontend/src/tests/__sentinel__.test.ts` — tiny passing test proving infra works:

    ```ts
    import { describe, it, expect } from 'vitest';
    describe('vitest infra sentinel', () => {
      it('jest-dom matchers available', () => {
        const el = document.createElement('div');
        el.textContent = 'hello';
        expect(el).toHaveTextContent('hello');
      });
      it('BroadcastChannel polyfill installed', () => {
        const bc = new BroadcastChannel('test');
        expect(bc.name).toBe('test');
        bc.close();
      });
    });
    ```

    8) Create `frontend/public/mockServiceWorker.js` — run `cd frontend && npx msw init public/ --save` to generate the canonical worker file. If the CLI is unavailable, copy from `node_modules/msw/lib/mockServiceWorker.js`. Do NOT hand-write.

    9) Append to `frontend/.env.example` (do not delete existing VITE_API_URL):

    ```
    # Phase 14 — frontend reads CSRF token from cookie; no env var needed
    # Backend FRONTEND_URL must be set to the URL serving this frontend
    # (see app/core/config.py AuthSettings.FRONTEND_URL — CORS allowlist)
    ```
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; bun run test 2>&amp;1 | grep -E "(2 pass|Tests.*passed|✓.*sentinel)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && bun run test` exits 0 (sentinel test passes)
    - `ls frontend/src/tests/setup.ts frontend/src/tests/msw/handlers.ts` — both exist
    - `ls frontend/src/tests/msw/auth.handlers.ts frontend/src/tests/msw/keys.handlers.ts frontend/src/tests/msw/ws.handlers.ts` — all exist
    - `ls frontend/public/mockServiceWorker.js` — exists
    - `grep -c "BroadcastChannel" frontend/src/tests/setup.ts` ≥ 1 (polyfill present per CONTEXT §146)
    - `grep -c "import.*handlers" frontend/src/tests/msw/handlers.ts` ≥ 3 (barrel re-exports 3 splits)
    - `grep -c "vitest" frontend/vitest.config.ts` ≥ 1
    - No nested-if: `grep -cE "^\s+if .*\bif\b" frontend/src/tests/setup.ts` returns 0
  </acceptance_criteria>
  <done>Vitest runs and passes sentinel; MSW handlers split per resource; jsdom polyfills (BroadcastChannel, location, clipboard) available globally; `/frontend-design` ready (shadcn primitives present from Task 1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev environment → npm registry | Untrusted package downloads |
| test runtime → MSW handlers | Mocked endpoints — no real backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | npm install | accept | bun.lock pinned; existing repo discipline |
| T-14-02 | Information disclosure | MSW handlers | accept | Mock data only; no real secrets |
</threat_model>

<verification>
- `cd frontend && bun install` succeeds with no errors
- `cd frontend && bun run test` exits 0 with the sentinel test passing
- `npx msw init public/ --save` produced the canonical mockServiceWorker.js
- shadcn primitives (form/input/label/dialog/alert) present alongside existing badge/button/card etc.
</verification>

<success_criteria>
- All 7 new deps + 5 new shadcn components present
- Vitest runs jsdom-environment tests with @testing-library/jest-dom matchers globally
- MSW Node server wired in setup.ts; handlers split per resource (auth/keys/ws); barrel reassembly in handlers.ts
- BroadcastChannel polyfill + writable window.location + navigator.clipboard mock all in place — Plans 03/05/06 depend on these
- Sentinel test proves the infra works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/14-atomic-frontend-cutover/14-01-SUMMARY.md`
</output>
