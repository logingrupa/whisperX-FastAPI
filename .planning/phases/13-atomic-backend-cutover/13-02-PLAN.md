---
phase: 13-atomic-backend-cutover
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/core/dual_auth.py
  - app/core/csrf_middleware.py
  - app/api/dependencies.py
autonomous: true
requirements: [MID-01, MID-02, MID-03, MID-04, AUTH-04]
must_haves:
  truths:
    - "DualAuthMiddleware accepts BOTH Authorization: Bearer whsk_* AND cookie session=<jwt>"
    - "On bearer auth: request.state.user, request.state.api_key_id, request.state.plan_tier, request.state.auth_method='bearer' set"
    - "On cookie auth: request.state.user, request.state.plan_tier, request.state.auth_method='cookie' set; cookie re-issued with refreshed exp (sliding 7d, AUTH-04)"
    - "Public path allowlist passes through with request.state.user=None"
    - "Missing/invalid auth on protected route returns 401 JSON {'detail': 'Authentication required'}"
    - "CsrfMiddleware enforces double-submit on POST/PUT/PATCH/DELETE for cookie-auth requests; bearer-auth skips CSRF"
    - "WebSocket route /ws/* exempt from middleware response wrapping"
    - "get_authenticated_user dependency reads request.state.user; raises 401 if None"
  artifacts:
    - path: "app/core/dual_auth.py"
      provides: "DualAuthMiddleware (cookie+bearer dual-auth)"
      contains: "class DualAuthMiddleware"
    - path: "app/core/csrf_middleware.py"
      provides: "CsrfMiddleware (double-submit enforcement)"
      contains: "class CsrfMiddleware"
    - path: "app/api/dependencies.py"
      provides: "get_authenticated_user, get_current_user_id, get_csrf_service helpers"
      exports: ["get_authenticated_user", "get_current_user_id"]
  key_links:
    - from: "app/core/dual_auth.py"
      to: "app/services/auth/key_service.py"
      via: "container.key_service().verify_plaintext(plaintext)"
      pattern: "key_service.*verify_plaintext"
    - from: "app/core/dual_auth.py"
      to: "app/services/auth/token_service.py"
      via: "container.token_service().verify_and_refresh(token, user.token_version)"
      pattern: "token_service.*verify_and_refresh"
    - from: "app/core/csrf_middleware.py"
      to: "app/core/csrf.py via CsrfService"
      via: "csrf_service.verify(cookie, header)"
      pattern: "csrf_service.*verify"
---

<objective>
Build DualAuthMiddleware + CSRF middleware as PURE middleware classes — wired into app/main.py in Plan 09 (atomic flip with route registration). This plan delivers the auth resolution + CSRF double-submit logic in isolation; no route side-effects.

Purpose: All Phase 13 routes (auth, keys, account, billing, scoped tasks) need request.state.user populated; this is the single resolution point per CONTEXT §46-56.
Output: 2 new middleware files + dependencies.py extension with `get_authenticated_user`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-atomic-backend-cutover/13-CONTEXT.md
@.planning/phases/11-auth-core-modules-services-di/11-04-SUMMARY.md
@app/core/auth.py
@app/core/container.py
@app/services/auth/auth_service.py
@app/services/auth/key_service.py
@app/services/auth/token_service.py
@app/services/auth/csrf_service.py
@app/api/dependencies.py
@app/domain/entities/user.py
@app/core/exceptions.py

<interfaces>
<!-- KeyService.verify_plaintext returns ApiKey on success; raises InvalidApiKeyHashError / InvalidApiKeyFormatError -->

```python
# app/services/auth/key_service.py
class KeyService:
    def verify_plaintext(self, plaintext: str) -> ApiKey:
        # Raises InvalidApiKeyFormatError, InvalidApiKeyHashError on failure
        ...

# app/services/auth/token_service.py
class TokenService:
    def verify_and_refresh(self, token: str, current_token_version: int) -> tuple[dict, str]:
        # Raises JwtExpiredError, JwtAlgorithmError, JwtTamperedError on failure
        # Returns (payload, new_token) for sliding refresh
        ...

# app/services/auth/csrf_service.py
class CsrfService:
    def verify(self, cookie_token: str, header_token: str) -> bool: ...

# app/domain/entities/user.py
@dataclass
class User:
    id: int | None
    email: str
    password_hash: str
    plan_tier: str = "trial"
    token_version: int = 0
    ...

# app/domain/entities/api_key.py
@dataclass
class ApiKey:
    id: int | None
    user_id: int
    name: str
    prefix: str
    hash: str
    revoked_at: datetime | None = None
    def is_active(self) -> bool: ...
```

<!-- Existing legacy middleware (analog for structure only — DELETE in Plan 09) -->

```python
# app/core/auth.py
class BearerAuthMiddleware(BaseHTTPMiddleware):
    PUBLIC_PATH_PREFIXES = ("/health", "/openapi.json", "/docs", "/redoc", "/static", "/favicon.ico")
    async def dispatch(self, request, call_next) -> Response: ...
```

<!-- Locked PUBLIC_ALLOWLIST from CONTEXT §54 (MID-03) -->

`/health`, `/health/live`, `/health/ready`, `/`, `/openapi.json`, `/docs`, `/redoc`, `/static`, `/favicon.ico`, `/auth/register`, `/auth/login`, `/ui/login`, `/ui/register`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DualAuthMiddleware in app/core/dual_auth.py</name>
  <read_first>
    - app/core/auth.py (lines 1-78, structural analog — DELETE LATER in Plan 09)
    - app/services/auth/key_service.py (verify_plaintext signature + raises)
    - app/services/auth/token_service.py (verify_and_refresh signature + raises)
    - app/core/exceptions.py (lines 612-715, auth exception types)
    - app/domain/entities/user.py (User shape)
    - app/core/container.py (key_service + token_service + user_repository providers)
  </read_first>
  <files>app/core/dual_auth.py, tests/unit/core/test_dual_auth.py</files>
  <behavior>
    Test cases (write tests/unit/core/test_dual_auth.py FIRST, RED then GREEN):

    - Test 1: Public path `/health` passes through; request.state.user is None; status 200
    - Test 2: Public path `/auth/register` (POST) passes through with no auth
    - Test 3: OPTIONS preflight on any path passes through (CORS)
    - Test 4: Authorization: Bearer whsk_<valid> sets request.state.auth_method='bearer'; request.state.user.id matches owning user; request.state.api_key_id matches the key's id; request.state.plan_tier matches user.plan_tier
    - Test 5: Authorization: Bearer whsk_<malformed> returns 401 JSON {"detail":"Authentication required"}
    - Test 6: Authorization: Bearer whsk_<unknown> (valid format, no DB match) returns 401
    - Test 7: Cookie session=<valid jwt> sets request.state.auth_method='cookie'; request.state.user populated; response Set-Cookie re-issues `session=<new>` with refreshed exp (AUTH-04 sliding)
    - Test 8: Cookie session=<expired jwt> returns 401
    - Test 9: Cookie session=<tampered jwt> returns 401
    - Test 10: Cookie session=<valid jwt but token_version mismatch> returns 401 (logout-all path)
    - Test 11: WebSocket scope (`scope["type"] == "websocket"`) bypasses middleware (call_next without auth check)
    - Test 12: Both bearer and cookie present → bearer wins (resolution order from CONTEXT §50)
    - Test 13: No auth on protected path returns 401 JSON
  </behavior>
  <action>
    **Create `app/core/dual_auth.py`** — Starlette `BaseHTTPMiddleware` subclass.

    Required imports (top of file):
    ```python
    from __future__ import annotations
    import logging
    from starlette.middleware.base import BaseHTTPMiddleware, RequestResponseEndpoint
    from starlette.requests import Request
    from starlette.responses import JSONResponse, Response
    from starlette.types import ASGIApp

    from app.core.config import get_settings
    from app.core.container import Container
    from app.core.exceptions import (
        InvalidApiKeyFormatError,
        InvalidApiKeyHashError,
        JwtAlgorithmError,
        JwtExpiredError,
        JwtTamperedError,
    )

    logger = logging.getLogger(__name__)
    ```

    Module constants:
    ```python
    PUBLIC_ALLOWLIST: tuple[str, ...] = (
        "/health",
        "/health/live",
        "/health/ready",
        "/",
        "/openapi.json",
        "/docs",
        "/redoc",
        "/static",
        "/favicon.ico",
        "/auth/register",
        "/auth/login",
        "/ui/login",
        "/ui/register",
    )
    SESSION_COOKIE = "session"
    CSRF_COOKIE = "csrf_token"
    BEARER_PREFIX = "Bearer "
    ```

    Helper functions (module-level, flat — no nested if):
    ```python
    def _is_public(path: str) -> bool:
        if path in PUBLIC_ALLOWLIST:
            return True
        for prefix in ("/static/", "/uploads/files/"):  # tus uploads need own auth via dual middleware too — keep narrow
            if path.startswith(prefix):
                return True
        return False

    def _unauthorized() -> JSONResponse:
        return JSONResponse(
            status_code=401,
            content={"detail": "Authentication required"},
            headers={"WWW-Authenticate": 'Bearer realm="whisperx"'},
        )
    ```

    `DualAuthMiddleware` class:
    ```python
    class DualAuthMiddleware(BaseHTTPMiddleware):
        """Resolve cookie session JWT OR whsk_* bearer; populate request.state.

        Resolution order (CONTEXT §50):
          1. Authorization: Bearer whsk_*  → bearer
          2. Cookie session=<jwt>          → cookie + sliding refresh
          3. Public path                   → pass-through
          4. Else                          → 401
        """

        def __init__(self, app: ASGIApp, *, container: Container) -> None:
            super().__init__(app)
            self._container = container

        async def dispatch(self, request: Request, call_next: RequestResponseEndpoint) -> Response:
            if request.method == "OPTIONS":
                return await call_next(request)

            # Bearer first (external API consumers)
            auth_header = request.headers.get("authorization", "")
            if auth_header.startswith(BEARER_PREFIX):
                return await self._dispatch_bearer(request, call_next, auth_header[len(BEARER_PREFIX):].strip())

            # Cookie session
            session_cookie = request.cookies.get(SESSION_COOKIE)
            if session_cookie:
                return await self._dispatch_cookie(request, call_next, session_cookie)

            # Public allowlist
            if _is_public(request.url.path):
                request.state.user = None
                request.state.plan_tier = None
                request.state.auth_method = None
                request.state.api_key_id = None
                return await call_next(request)

            return _unauthorized()

        async def _dispatch_bearer(
            self,
            request: Request,
            call_next: RequestResponseEndpoint,
            plaintext: str,
        ) -> Response:
            try:
                api_key = self._container.key_service().verify_plaintext(plaintext)
            except (InvalidApiKeyFormatError, InvalidApiKeyHashError):
                return _unauthorized()

            user_repo = self._container.user_repository()
            user = user_repo.get_by_id(api_key.user_id)
            if user is None:
                return _unauthorized()

            request.state.user = user
            request.state.plan_tier = user.plan_tier
            request.state.auth_method = "bearer"
            request.state.api_key_id = api_key.id
            return await call_next(request)

        async def _dispatch_cookie(
            self,
            request: Request,
            call_next: RequestResponseEndpoint,
            token: str,
        ) -> Response:
            try:
                # Decode without token-version check first to recover sub
                from app.core import jwt_codec
                settings = get_settings()
                secret = settings.auth.JWT_SECRET.get_secret_value()
                payload = jwt_codec.decode_session(token, secret=secret)
                user_id = int(payload["sub"])
            except (JwtExpiredError, JwtAlgorithmError, JwtTamperedError, KeyError, ValueError):
                return _unauthorized()

            user_repo = self._container.user_repository()
            user = user_repo.get_by_id(user_id)
            if user is None:
                return _unauthorized()

            # Verify token_version + issue refreshed token (AUTH-04 sliding)
            try:
                _, new_token = self._container.token_service().verify_and_refresh(token, user.token_version)
            except (JwtExpiredError, JwtAlgorithmError, JwtTamperedError):
                return _unauthorized()

            request.state.user = user
            request.state.plan_tier = user.plan_tier
            request.state.auth_method = "cookie"
            request.state.api_key_id = None

            response = await call_next(request)
            self._set_session_cookie(response, new_token)
            return response

        def _set_session_cookie(self, response: Response, token: str) -> None:
            settings = get_settings()
            response.set_cookie(
                key=SESSION_COOKIE,
                value=token,
                max_age=settings.auth.JWT_TTL_DAYS * 24 * 3600,
                httponly=True,
                secure=settings.auth.COOKIE_SECURE,
                samesite="lax",
                path="/",
                domain=settings.auth.COOKIE_DOMAIN or None,
            )
    ```

    **Constraints (verifier-checked):**
    - SRP — middleware does auth resolution ONLY; no business logic, no DB writes
    - DRY — `_unauthorized()` reused; cookie set helper extracted
    - No nested-if (`grep -cE "^\s+if .*\bif\b" app/core/dual_auth.py` returns 0)
    - Self-explanatory names — DualAuthMiddleware, _dispatch_bearer, _dispatch_cookie
    - Logging discipline — log only `auth_method=, user_id=`; never JWT, never plaintext
    - WebSocket scopes (`scope["type"] == "websocket"`) — Starlette BaseHTTPMiddleware only handles `http`, so WS bypasses naturally; document this in module docstring

    Then write tests at `tests/unit/core/test_dual_auth.py` using `httpx.AsyncClient` + `Starlette` test app + mocked Container providers (override `container.key_service`, `container.token_service`, `container.user_repository`).
  </action>
  <verify>
    <automated>pytest tests/unit/core/test_dual_auth.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `app/core/dual_auth.py` exists
    - `grep -c "class DualAuthMiddleware" app/core/dual_auth.py` returns 1
    - `grep -c "PUBLIC_ALLOWLIST" app/core/dual_auth.py` returns ≥2
    - `grep -c "/auth/register" app/core/dual_auth.py` returns ≥1
    - `grep -c "/auth/login" app/core/dual_auth.py` returns ≥1
    - `grep -c "auth_method.*bearer" app/core/dual_auth.py` returns ≥1
    - `grep -c "auth_method.*cookie" app/core/dual_auth.py` returns ≥1
    - `grep -c "set_cookie" app/core/dual_auth.py` returns ≥1 (sliding refresh)
    - `grep -cE "^\s+if .*\bif\b" app/core/dual_auth.py` returns 0
    - `grep -c "jwt.decode(" app/core/dual_auth.py` returns 0 (only jwt_codec.py allowed)
    - `pytest tests/unit/core/test_dual_auth.py -v` passes ≥10 tests
  </acceptance_criteria>
  <done>DualAuthMiddleware functional + tested; not yet wired into app/main.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CsrfMiddleware in app/core/csrf_middleware.py + dependencies.get_authenticated_user</name>
  <read_first>
    - app/services/auth/csrf_service.py
    - app/api/dependencies.py
    - app/core/dual_auth.py (from Task 1 — request.state contract)
  </read_first>
  <files>app/core/csrf_middleware.py, app/api/dependencies.py, tests/unit/core/test_csrf_middleware.py</files>
  <behavior>
    Test cases for CsrfMiddleware (RED→GREEN):
    - Test 1: GET request with cookie auth + no CSRF header → passes (CSRF only on state-mutating)
    - Test 2: POST with auth_method='cookie' + valid X-CSRF-Token matching csrf_token cookie → passes
    - Test 3: POST with auth_method='cookie' + missing X-CSRF-Token → 403 JSON {"detail":"CSRF token missing"}
    - Test 4: POST with auth_method='cookie' + mismatched X-CSRF-Token → 403 JSON {"detail":"CSRF token mismatch"}
    - Test 5: POST with auth_method='bearer' → SKIP CSRF (passes without header)
    - Test 6: PUT, PATCH, DELETE with cookie auth all enforce CSRF
    - Test 7: OPTIONS preflight always passes
    - Test 8: Public path (no auth_method) → SKIP CSRF
    - Test 9: WebSocket scope bypasses

    Test cases for `get_authenticated_user`:
    - Test 1: request.state.user populated → returns User
    - Test 2: request.state.user is None → raises HTTPException(401)
  </behavior>
  <action>
    **2a. Create `app/core/csrf_middleware.py`** — runs AFTER DualAuthMiddleware (so request.state.auth_method is set):

    ```python
    """Double-submit CSRF middleware.

    Per CONTEXT §55 + MID-04:
      - GET/HEAD/OPTIONS — bypass.
      - auth_method == 'bearer' — bypass (external API clients).
      - auth_method == 'cookie' on POST/PUT/PATCH/DELETE — verify
        X-CSRF-Token header == csrf_token cookie via secrets.compare_digest.
      - auth_method is None — bypass (public allowlist; no state to mutate).

    Wired AFTER DualAuthMiddleware in app/main.py so request.state.auth_method
    is populated before this runs.
    """
    from __future__ import annotations
    import logging
    from starlette.middleware.base import BaseHTTPMiddleware, RequestResponseEndpoint
    from starlette.requests import Request
    from starlette.responses import JSONResponse, Response
    from starlette.types import ASGIApp

    from app.core.container import Container

    logger = logging.getLogger(__name__)
    STATE_MUTATING_METHODS = frozenset({"POST", "PUT", "PATCH", "DELETE"})
    CSRF_COOKIE = "csrf_token"
    CSRF_HEADER = "x-csrf-token"


    def _csrf_error(detail: str) -> JSONResponse:
        return JSONResponse(status_code=403, content={"detail": detail})


    class CsrfMiddleware(BaseHTTPMiddleware):
        def __init__(self, app: ASGIApp, *, container: Container) -> None:
            super().__init__(app)
            self._container = container

        async def dispatch(self, request: Request, call_next: RequestResponseEndpoint) -> Response:
            if request.method not in STATE_MUTATING_METHODS:
                return await call_next(request)

            auth_method = getattr(request.state, "auth_method", None)
            if auth_method != "cookie":
                # bearer or public allowlist — skip CSRF
                return await call_next(request)

            cookie_token = request.cookies.get(CSRF_COOKIE, "")
            header_token = request.headers.get(CSRF_HEADER, "")
            if not header_token:
                return _csrf_error("CSRF token missing")
            if not self._container.csrf_service().verify(cookie_token, header_token):
                return _csrf_error("CSRF token mismatch")
            return await call_next(request)
    ```

    Constraints: flat guards (no nested if); SRP — CSRF only; no business logic.

    **2b. Extend `app/api/dependencies.py`** — append at the end (preserve existing helpers):

    ```python
    # ---------------------------------------------------------------
    # Phase 13 — Auth dependencies (request.state populated by DualAuthMiddleware)
    # ---------------------------------------------------------------
    from fastapi import HTTPException, Request, status
    from app.domain.entities.user import User
    from app.services.auth import CsrfService, KeyService, AuthService, RateLimitService


    def get_authenticated_user(request: Request) -> User:
        """Resolve the authenticated user from request.state (DualAuthMiddleware-set).

        Use this on every protected route via `Depends(get_authenticated_user)`.
        Raises 401 if no user (defense-in-depth — middleware should already 401).
        """
        user: User | None = getattr(request.state, "user", None)
        if user is None:
            raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Authentication required")
        return user


    def get_current_user_id(request: Request) -> int:
        """Convenience: just the integer id."""
        user = get_authenticated_user(request)
        return int(user.id)


    def get_csrf_service() -> CsrfService:
        if _container is None:
            raise RuntimeError(CONTAINER_NOT_INITIALIZED_ERROR)
        return _container.csrf_service()


    def get_key_service() -> KeyService:
        if _container is None:
            raise RuntimeError(CONTAINER_NOT_INITIALIZED_ERROR)
        return _container.key_service()


    def get_auth_service() -> AuthService:
        if _container is None:
            raise RuntimeError(CONTAINER_NOT_INITIALIZED_ERROR)
        return _container.auth_service()


    def get_rate_limit_service() -> RateLimitService:
        if _container is None:
            raise RuntimeError(CONTAINER_NOT_INITIALIZED_ERROR)
        return _container.rate_limit_service()
    ```

    DRT — `Depends(get_authenticated_user)` reused across all 4 new route files (auth, keys, account, billing).

    **2c. Tests** at `tests/unit/core/test_csrf_middleware.py` exercising all 9 behaviors using a Starlette test app with manual `request.state.auth_method` injection via a tiny preceding middleware.
  </action>
  <verify>
    <automated>pytest tests/unit/core/test_csrf_middleware.py tests/unit/api/test_exception_handlers.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `app/core/csrf_middleware.py` exists; `grep -c "class CsrfMiddleware" app/core/csrf_middleware.py` returns 1
    - `grep -c "STATE_MUTATING_METHODS" app/core/csrf_middleware.py` returns ≥1
    - `grep -c 'auth_method != "cookie"' app/core/csrf_middleware.py` returns 1
    - `grep -cE "^\s+if .*\bif\b" app/core/csrf_middleware.py` returns 0
    - `grep -c "def get_authenticated_user" app/api/dependencies.py` returns 1
    - `grep -c "def get_csrf_service" app/api/dependencies.py` returns 1
    - `grep -c "def get_key_service" app/api/dependencies.py` returns 1
    - `grep -c "def get_auth_service" app/api/dependencies.py` returns 1
    - `grep -c "def get_rate_limit_service" app/api/dependencies.py` returns 1
    - `pytest tests/unit/core/test_csrf_middleware.py -v` passes ≥9 tests
    - `pytest tests/` regression: no new failures
  </acceptance_criteria>
  <done>CsrfMiddleware tested; dependencies.py exposes 5 new auth helpers; not yet wired into app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→FastAPI | Cookies + JWT + CSRF token cross here; untrusted |
| External client→FastAPI | Bearer whsk_* tokens cross here; untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Spoofing | DualAuthMiddleware bearer leg | mitigate | KeyService.verify_plaintext uses secrets.compare_digest on SHA-256 hash + indexed prefix (KEY-08); InvalidApiKeyHashError → 401 |
| T-13-02 | Spoofing | DualAuthMiddleware cookie leg | mitigate | jwt_codec.decode_session enforces algorithms=["HS256"]; tampered/expired/wrong-alg → typed exception → 401 |
| T-13-03 | Tampering | Cookie token_version replay | mitigate | TokenService.verify_and_refresh compares payload['ver'] against user.token_version; mismatch → JwtTamperedError → 401 |
| T-13-04 | Repudiation | Auth event logging | mitigate | DualAuthMiddleware logs auth_method + user_id only; NEVER raw token, NEVER plaintext key |
| T-13-05 | Information Disclosure | 401 detail message | mitigate | Single error string "Authentication required" — no leak about which leg failed |
| T-13-06 | Tampering | CSRF cross-site write | mitigate | CsrfMiddleware verifies double-submit on cookie POST/PUT/PATCH/DELETE; secrets.compare_digest |
| T-13-07 | Denial of Service | DualAuthMiddleware DB lookup per request | accept | KEY-08 idx_api_keys_prefix + user_repository.get_by_id(int PK) — both O(1); no query unbound by index |
</threat_model>

<verification>
  <automated>pytest tests/unit/core/test_dual_auth.py tests/unit/core/test_csrf_middleware.py -v</automated>
</verification>

<success_criteria>
- DualAuthMiddleware handles bearer + cookie + public + 401 paths via dedicated helpers
- CsrfMiddleware enforces double-submit ONLY on cookie-auth state-mutating requests
- request.state.{user, plan_tier, auth_method, api_key_id} contract is the SINGLE source of auth context (DRT)
- 5 new dependencies in app/api/dependencies.py reused across all Phase 13 routes
- No `jwt.decode(` outside `app/core/jwt_codec.py` (verified by grep)
- No nested-if anywhere (`grep -cE "^\s+if .*\bif\b" app/core/dual_auth.py app/core/csrf_middleware.py` returns 0)
- ≥19 unit tests pass across both middleware files
- Existing tests still pass (regression-free)
</success_criteria>

<output>
After completion, create `.planning/phases/13-atomic-backend-cutover/13-02-SUMMARY.md` listing files created, public-allowlist contents, request.state contract, and the dependencies API surface.
</output>
