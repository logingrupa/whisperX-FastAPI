---
phase: quick-260722-1uh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - app/infrastructure/ml/whisperx_diarization_service.py
  - start-server-boot.bat
  - start-server.bat
autonomous: false
requirements: [Q1UH-01, Q1UH-02, Q1UH-03, Q1UH-04, Q1UH-05]

must_haves:
  truths:
    - "torchcodec AudioDecoder imports cleanly against FFmpeg 7.1 shared DLLs (test venv AND production venv)"
    - "torch.cuda.is_available() is True after every install/sync step — never clobbered by CPU wheels"
    - "Transcribe+align works for en, lv (models\\raivis-small-lv-ct2-fp16), ru (models\\faster-whisper-large-v3-russian) with word coverage and zero-word-segment count not regressed vs 3.7.4 baseline"
    - "Diarization runs under pyannote.audio 4.0.7 with pyannote/speaker-diarization-3.1 in HF_HUB_OFFLINE=1 mode; POST /service/diarize returns [{label,speaker,start,end}]"
    - "Production backend restarted on empty queue OR written no-adopt recommendation with measurements — unvalidated promotion never happens"
  artifacts:
    - path: "pyproject.toml"
      provides: "whisperx pinned to git SHA 2cfd7b7c5c7bba144954364db747319b50e8232b + AMD64 CUDA markers + torchvision source"
      contains: "2cfd7b7c5c7bba144954364db747319b50e8232b"
    - path: "app/infrastructure/ml/whisperx_diarization_service.py"
      provides: "DiarizationPipeline(model_name='pyannote/speaker-diarization-3.1', token=...) — pyannote 4.x API"
      contains: "model_name"
    - path: "start-server-boot.bat"
      provides: "FFMPEG_DIR pinned to C:\\tools\\ffmpeg-7.1-shared\\bin"
      contains: "ffmpeg-7.1-shared"
  key_links:
    - from: "start-server-boot.bat"
      to: "torchcodec DLL discovery + whisperx ffmpeg subprocess"
      via: "PATH prepend of FFmpeg 7.1 shared bin"
      pattern: "ffmpeg-7.1-shared"
    - from: "pyproject.toml [tool.uv.sources]"
      to: "cu128 torch wheels on Windows"
      via: "platform_machine == 'AMD64' marker"
      pattern: "AMD64"
    - from: "app/infrastructure/ml/whisperx_diarization_service.py"
      to: "cached pyannote/speaker-diarization-3.1"
      via: "explicit model_name (main defaults to uncached community-1)"
      pattern: "speaker-diarization-3\\.1"
---

<objective>
Adopt whisperX git main (pinned SHA `2cfd7b7c5c7bba144954364db747319b50e8232b`) in the production venv. Resolve the torchcodec/FFmpeg incompatibility first (FFmpeg 8.1 static → 7.1 shared), validate everything in the existing test venv (transcription en/lv/ru parity, diarization under pyannote 4.0.7 — the highest-risk gate), fix the CPU-wheel-clobber root cause in this repo's uv markers, patch the one breaking app call site, then promote and restart on an empty queue.

Purpose: main drops backtrack_beam (faster, equal quality per prior validated session) and unblocks future upgrades; 3.7.4 is frozen on old pyannote 3.x.
Output: EITHER promoted production venv (pyproject pinned to SHA, all validations green, backend restarted) OR a written no-adopt recommendation with measurements. Unvalidated promotion is forbidden.
</objective>

<execution_context>
@C:/Users/rolan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rolan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260722-1uh-adopt-whisperx-main-pinned-sha-fix-torch/260722-1uh-RESEARCH.md
@pyproject.toml
@start-server-boot.bat
@app/infrastructure/ml/whisperx_diarization_service.py

Key facts (from RESEARCH.md, all VERIFIED unless noted):
- whisperX main = 3.8.7rc1; pins torch~=2.8.0, pyannote-audio>=4.0.0, torchcodec>=0.6,<0.8 → resolver picks torchcodec 0.7.x → FFmpeg majors 4-7 ONLY. Box's gyan.dev 8.1 static build fails both axes (no shared DLLs + wrong major).
- FFmpeg fix: BtbN shared build `https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-n7.1-latest-win64-gpl-shared-7.1.zip` (~71.5 MB, rolling tag — record sha256 after download). Vendor to `C:\tools\ffmpeg-7.1-shared\`. Shared build ships ffmpeg.exe too, so whisperx.load_audio's bare-`ffmpeg` subprocess keeps working from the same dir.
- CPU-wheel clobber ROOT CAUSE: pyproject.toml:77-87 markers use `platform_machine == 'x86_64'`; Windows reports `AMD64` → torch resolves from pytorch-cpu index. Mirror upstream whisperX commit `11bc7de` (add AMD64 to markers). torchvision has NO sources entry at all (resolves CPU wheel from PyPI) — must add.
- App break: whisperx_diarization_service.py:29 passes `use_auth_token=` → TypeError on main (`DiarizationPipeline(model_name=None, token=None, device=None, cache_dir=None)`). Main's default model is `pyannote/speaker-diarization-community-1` — NOT cached, gated, and HF_HUB_OFFLINE=1 → silent fail. MUST pass `model_name="pyannote/speaker-diarization-3.1"`.
- All other whisperx call sites compatible: load_model, align/load_align_model, assign_word_speakers, load_audio, SAMPLE_RATE (RESEARCH §7).
- MEDIUM-confidence gate: 3.1 checkpoint loading under pyannote 4.0.7. THE validation gate. Fallback if it fails: unset HF_HUB_OFFLINE, accept gated conditions for community-1, warm cache, re-enable offline, A/B quality — but that is a checkpoint:decision for the owner, not automatic.
- Existing test venv: `C:\Users\rolan\AppData\Local\Temp\claude\C--laragon-www-whisperx\b0f54f43-1741-4c74-8545-4b40bfeca529\scratchpad\venv-wsp-main` (whisperx 3.8.7rc1 + torch 2.8.0+cu128 CUDA working; torchcodec broken pending FFmpeg).
- Operational: backend = SYSTEM scheduled task `\WhisperX Backend`; restart requires ELEVATED Stop-ScheduledTask/Start-ScheduledTask. records.db timestamps UTC (machine UTC+3). HF_HUB_OFFLINE=1 + TRANSFORMERS_OFFLINE=1 in start-server-boot.bat:37-38.
- nltk punkt_tab: main uses it for sentence splitting; offline box should pre-download once (Pitfall 6, ASSUMED codepath).
</context>

<tasks>

<task type="auto">
  <name>Task 1: FFmpeg 7.1 shared install + full test-venv validation (torchcodec, diarization gate, en/lv/ru parity, timings)</name>
  <files>C:\tools\ffmpeg-7.1-shared\ (new, outside repo), scratchpad validation scripts only — NO repo files modified in this task</files>
  <action>
**1a. Capture 3.7.4 baselines FIRST (production .venv, read-only — do NOT modify it):**
- Pick one known sample audio file per language (en, lv, ru). Check the prior-session scratchpad `C:\Users\rolan\AppData\Local\Temp\claude\C--laragon-www-whisperx\b0f54f43-1741-4c74-8545-4b40bfeca529\scratchpad\` for the audio files + outputs already used for parity; reuse them. If absent, use any short (<2 min) known-language samples available on the box (ask owner via checkpoint only if literally none found).
- For each language, in the PRODUCTION `.venv` python, run transcribe+align (lv → `models\raivis-small-lv-ct2-fp16`, ru → `models\faster-whisper-large-v3-russian`, en → default) and record: (1) count of words having both start+end, (2) count of segments with zero words, (3) wall-clock `whisperx.load_model` time (time.perf_counter around the call, 2 runs, report both — first is cold). Save baselines as JSON in this session's scratchpad. Set HF_HOME=C:\Users\rolan\.cache\huggingface and HF_HUB_OFFLINE=1 in the env for these runs (match server env).

**1b. Install FFmpeg 7.1 shared:**
- Download `https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-n7.1-latest-win64-gpl-shared-7.1.zip`, record sha256 (Get-FileHash), extract, vendor `bin/` contents to `C:\tools\ffmpeg-7.1-shared\bin\`. Do NOT delete or touch the existing 8.1 winget dir (rollback path = old PATH still valid).
- Sanity: `C:\tools\ffmpeg-7.1-shared\bin\ffmpeg.exe -version` reports major 7.1 and dir contains avcodec-61*.dll, avformat-61*.dll, avutil-59*.dll.

**1c. Test-venv DLL + import gate** (test venv = `...\b0f54f43...\scratchpad\venv-wsp-main`; prepend `C:\tools\ffmpeg-7.1-shared\bin` to PATH for every command in this task):
- `python -c "from torchcodec.decoders import AudioDecoder; print('OK')"` → OK
- `python -c "from pyannote.audio import Pipeline; print('OK')"` → OK
- `python -c "import torch; assert torch.cuda.is_available(); print('CUDA OK')"` → CUDA OK

**1d. THE diarization gate (3.1 checkpoint under pyannote 4.0.7, offline):** with HF_HOME + HF_HUB_OFFLINE=1 set, run in test venv:
```python
from whisperx.diarize import DiarizationPipeline
import whisperx, time
p = DiarizationPipeline(model_name="pyannote/speaker-diarization-3.1", token="<HF_TOKEN from .env>", device="cuda")
audio = whisperx.load_audio(r"<known 2-speaker file>")
df = p(audio=audio, min_speakers=2, max_speakers=2)
print(df.columns.tolist(), len(df))
assert {"label", "speaker", "start", "end"} <= set(df.columns) or ({"segment","label","speaker"} <= set(df.columns)), df.columns
assert len(df) > 0 and df["speaker"].nunique() >= 2
```
Note: main returns DataFrame with segment/label/speaker; start/end may derive from segment — inspect actual columns and record the exact shape (Task 2 needs it to confirm the API response mapping in app code still yields [{label,speaker,start,end}]).
- IF THIS FAILS (checkpoint load error under 4.0.7): STOP. Do not promote. Write the no-adopt recommendation in SUMMARY with the exact error, note the community-1 fallback option (requires going online + gated-model acceptance — owner decision), and skip Tasks 2-3.

**1e. Transcription parity smoke (test venv, same audio as 1a):** run en/lv/ru transcribe+align, compare against 1a baselines: word-coverage count >= baseline (small tolerance: no more than 2% fewer words with timestamps), zero-word-segment count <= baseline. Record load_model timings same way (2 runs each).

**1f. Reference freeze:** `pip freeze` from test venv saved to scratchpad — Task 2 diffs uv.lock resolution against it (transformers/ctranslate2/faster-whisper major drift = re-smoke trigger).
  </action>
  <verify>
    <automated>PATH-prepended test-venv: python -c "from torchcodec.decoders import AudioDecoder; print('OK')" && python -c "import torch; assert torch.cuda.is_available()"; diarization script exits 0 with >=2 speakers; parity JSON shows no regression vs 3.7.4 baseline JSON</automated>
  </verify>
  <done>FFmpeg 7.1 shared vendored at C:\tools\ffmpeg-7.1-shared (8.1 dir untouched); torchcodec + pyannote import OK in test venv; diarization-3.1-under-4.0.7 gate PASSED (or task aborted with written findings); en/lv/ru parity confirmed vs fresh 3.7.4 baselines; load_model timings + pip freeze captured in scratchpad</done>
</task>

<task type="auto">
  <name>Task 2: Repo changes (SHA pin + AMD64 markers + diarization patch + .bat edits) → production .venv promotion + venv-level smoke</name>
  <files>pyproject.toml, uv.lock, app/infrastructure/ml/whisperx_diarization_service.py, start-server-boot.bat, start-server.bat</files>
  <action>
PRECONDITION: Task 1 fully green. If Task 1 aborted, skip to SUMMARY (no-adopt report).

**2a. pyproject.toml — one atomic edit (marker fix MUST land in same change as the pin, Pitfall 1):**
- Replace `"whisperx==3.7.4"` with `"whisperx"` in [project.dependencies].
- Add to [tool.uv.sources]: `whisperx = { git = "https://github.com/m-bain/whisperX", rev = "2cfd7b7c5c7bba144954364db747319b50e8232b" }`.
- Fix torch + torchaudio marker triples (pyproject.toml:78-87), mirroring upstream commit 11bc7de:
  - `{ index = "pytorch-cpu", marker = "sys_platform == 'darwin'" }`
  - `{ index = "pytorch", marker = "(platform_machine == 'x86_64' or platform_machine == 'AMD64') and sys_platform != 'darwin'" }`
  - `{ index = "pytorch-cpu", marker = "platform_machine != 'x86_64' and platform_machine != 'AMD64' and sys_platform != 'darwin'" }`
- ADD identical three-entry sources block for `torchvision` (currently missing entirely — resolves CPU wheel from PyPI).
- Keep torch<=2.8.0 / torchaudio<=2.8.0 / torchvision<=0.23.0 pins (intersect cleanly with main's ~= pins).

**2b. Diarization service patch** (app/infrastructure/ml/whisperx_diarization_service.py:29):
```python
pipeline = DiarizationPipeline(
    model_name="pyannote/speaker-diarization-3.1",
    token=hf_token,
    device=device,
)
```
Keep the fail-loud None-guard wrapper (_load_pipeline lines 30-37) — pyannote 4 from_pretrained can still return None-ish failures. Update _GATED_MODEL_HINT text only if it references stale API terms. If Task 1d revealed the returned DataFrame lacks flat start/end columns, adapt whatever app-layer mapping produces the /service/diarize response so it still emits [{label,speaker,start,end}] (check app/api/audio_services_api.py diarize route + whisperx_speaker_assignment_service.py consumer — do NOT change response schema).

**2c. Both .bat edits:**
- start-server-boot.bat:18 → `set "FFMPEG_DIR=C:\tools\ffmpeg-7.1-shared\bin"` (leave PATH-prepend line 19 as is; leave HF_HUB_OFFLINE block untouched).
- start-server.bat → replace winget ffmpeg auto-discovery loop with pinned `C:\tools\ffmpeg-7.1-shared\bin` prepend (auto-discovery would find 8.1 static and break torchcodec in dev shells).

**2d. Promote to production .venv** (queue does NOT matter yet — server keeps running the old in-memory code until Task 3 restart; on-disk .venv swap is safe because worker imports are already resolved, but do this in a low-traffic moment anyway):
- Backup first: `Move-Item .venv .venv-374-backup` is NOT possible while server runs (file locks). Instead: `uv sync` in-place is also locked-risk. Safest: check queue idle-ish, then `uv sync` — Windows file locks on loaded .pyd files will fail the sync if a job is actively importing; retry when `sqlite3 records.db "select count(*) from tasks where status='processing'"` = 0. If sync still hits locks, stop the scheduled task FIRST (pull Task 3's stop step forward — elevated `Stop-ScheduledTask -TaskName "\WhisperX Backend"` after confirming 0 processing) and then sync.
- `uv sync` (git must be on PATH). Then the non-negotiable gate: `.venv\Scripts\python -c "import torch; assert torch.cuda.is_available(); print(torch.__version__)"` → must print 2.8.0+cu128. If CPU wheel appears: markers wrong — fix, re-sync; do NOT band-aid with force-reinstall.
- `python -c "import whisperx; print(whisperx.__version__)"` → 3.8.7rc1.
- Diff `pip freeze` vs Task 1f reference: if transformers, ctranslate2, or faster-whisper differ in MAJOR/MINOR → re-run the Task 1e parity smoke inside the production .venv before proceeding.
- With FFmpeg 7.1 dir on PATH: `python -c "from torchcodec.decoders import AudioDecoder; print('OK')"` in production .venv.
- Pre-download nltk data (one-time, with offline vars UNSET for this command only): `python -c "import nltk; nltk.download('punkt_tab')"` (Pitfall 6).
- Production-.venv smoke with HF_HUB_OFFLINE=1: one transcribe+align (en) + the Task 1d diarization script. Both must pass.

**Rollback path (if anything in 2d fails unrecoverably):** `git checkout -- pyproject.toml uv.lock app/infrastructure/ml/whisperx_diarization_service.py start-server-boot.bat start-server.bat` then `uv sync` → restores whisperx==3.7.4 venv; old FFmpeg 8.1 dir still intact and referenced by reverted .bat. Record failure in SUMMARY as no-adopt recommendation.

**Commits (atomic, do NOT commit .planning/):**
1. `feat(deps): pin whisperx to main SHA 2cfd7b7; fix AMD64 CUDA markers + torchvision source` — pyproject.toml + uv.lock
2. `fix(diarization): pyannote 4.x API — token= + explicit speaker-diarization-3.1 model` — whisperx_diarization_service.py (+ any mapping adaptation)
3. `chore(ops): pin FFmpeg 7.1 shared in both server launchers (torchcodec DLL compat)` — both .bat files
  </action>
  <verify>
    <automated>.venv\Scripts\python -c "import torch; assert torch.cuda.is_available()" && .venv\Scripts\python -c "import whisperx; assert whisperx.__version__.startswith('3.8')" && (PATH-prepended) .venv\Scripts\python -c "from torchcodec.decoders import AudioDecoder; print('OK')"; diarization smoke exits 0 offline</automated>
  </verify>
  <done>pyproject pinned to SHA with AMD64 markers + torchvision source; diarization service on token=/model_name= API; both .bat files pin FFmpeg 7.1 shared; production .venv synced with CUDA intact; freeze diff checked (re-smoked if drifted); en transcription + diarization pass in production .venv offline; 3 atomic commits made</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Elevated restart on empty queue + production API smoke + measurements report</name>
  <files>none (operational + SUMMARY content)</files>
  <action>
**3a. Automated pre-restart guard (Claude runs this):**
- `sqlite3 records.db "select count(*) from tasks where status='processing'"` MUST return 0. If >0, wait and re-check (timestamps in records.db are UTC; machine is UTC+3 — never compare against local clock). Do not restart with jobs in flight.

**3b. Restart (requires elevation — Claude ATTEMPTS first, hands off only on AccessDenied):**
```powershell
Stop-ScheduledTask -TaskName "\WhisperX Backend"
Start-Sleep -Seconds 5
Start-ScheduledTask -TaskName "\WhisperX Backend"
```
If Claude's shell lacks elevation (Access is denied): present these exact commands to the owner for an elevated PowerShell. (Skip Stop if Task 2d already stopped it for file locks — then only Start.)

**3c. Post-restart verification (Claude, automated):**
- Tail `logs\backend-boot.log` — uvicorn boots clean, no import errors, no torchcodec/DLL errors.
- `curl http://localhost:8000/health` (or the app's health route) → 200.
- Submit one real transcription job end-to-end through the API (en sample) → completes with word timestamps. This also proves ffmpeg.exe subprocess path under the new FFMPEG_DIR in the SYSTEM environment.
- POST /service/diarize with the 2-speaker file → response is [{label,speaker,start,end}] with >=2 distinct speakers.

**3d. Measurements report (goes in SUMMARY — report-only, per owner):**
- load_model timing table: 3.7.4 baseline (Task 1a) vs main (Task 1e / production runs), per model.
- If main still 3-5s slower: state plainly. Note the mitigation option: WhisperXTranscriptionService already has unused load_model/unload_model residency hooks (whisperx_transcription_service.py:147-199) — caching loaded model keyed (model, language, compute_type) would erase the regression; NOT implemented now (out of scope, owner may schedule).
- Word-coverage parity table en/lv/ru: baseline vs main.
- FFmpeg 7.1 shared sha256 recorded; 8.1 dir left intact at its winget path (rollback).
  </action>
  <verify>
    <automated>sqlite3 records.db "select count(*) from tasks where status='processing'" returns 0 before restart; curl health 200 after restart; API transcription job completes; /service/diarize returns non-empty [{label,speaker,start,end}]</automated>
  </verify>
  <done>Backend restarted (elevated) on empty queue; boot log clean; one API transcription + one API diarization succeed under SYSTEM env with FFmpeg 7.1 + HF_HUB_OFFLINE=1; SUMMARY contains load-time comparison, parity table, and residency-cache note</done>
  <resume-signal>Owner confirms elevated restart executed (only if Claude's automated attempt was AccessDenied); otherwise Claude proceeds automatically</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Third-party code (git dep) | whisperX installed from GitHub — SHA pin is the integrity control |
| Binary download | BtbN FFmpeg zip from GitHub releases — sha256 recorded at vendor time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q1uh-01 | Tampering | whisperx git dependency | mitigate | Pin exact rev `2cfd7b7c...` in [tool.uv.sources]; uv.lock records SHA — no floating branch |
| T-q1uh-02 | Tampering | FFmpeg 7.1 zip (rolling `latest` tag) | mitigate | Record sha256 immediately after download in SUMMARY; vendored to fixed local path |
| T-q1uh-03 | Info disclosure | HF_TOKEN in validation scripts | mitigate | Read token from .env at runtime; never hardcode into committed files or SUMMARY |
| T-q1uh-04 | DoS | Restart with jobs processing | mitigate | Explicit `status='processing'` count==0 gate before Stop-ScheduledTask |
</threat_model>

<verification>
- torchcodec import OK in test venv AND production .venv with FFmpeg 7.1 shared on PATH
- torch.cuda.is_available() True after uv sync (no CPU clobber — markers fixed at root cause)
- en/lv/ru transcribe+align parity vs freshly captured 3.7.4 baselines (word coverage, zero-word segments)
- Diarization 3.1-under-pyannote-4.0.7 offline gate passed in test venv, production .venv, and via live POST /service/diarize
- Backend restarted elevated on empty queue; API-level transcription + diarization succeed under SYSTEM environment
- FFmpeg 8.1 dir intact (rollback preserved); git-revert + uv sync documented as venv rollback
</verification>

<success_criteria>
EITHER: pyproject pinned to SHA 2cfd7b7c, all validations green, 3 atomic commits, backend restarted and serving jobs on whisperX main — with SUMMARY containing load-time comparison, parity table, ffmpeg sha256, and residency-cache recommendation note.
OR: validation failed at a gate (most likely 1d diarization) → repo untouched or rolled back, production still on 3.7.4, SUMMARY contains written no-adopt recommendation with exact error + measurements + community-1 fallback option for owner decision.
</success_criteria>

<output>
After completion, create `.planning/quick/260722-1uh-adopt-whisperx-main-pinned-sha-fix-torch/260722-1uh-SUMMARY.md` (do NOT commit .planning docs — orchestrator handles).
</output>
